/* Browser-local persistence and portable, auditable customer work packages. */
(function customerWorkspaceRuntime(){
'use strict';
const DB_NAME='ghgy-customer-workspace',DB_VERSION=4,RECORD_STORE='records',META_STORE='meta',CHART_STORE='chart-records-v2',DERIVED_STORE='derived-records-v2',MEDIA_STORE='media-records-v1';
const SCHEMA_VERSION='ghgy-workspace-v7';
const OBJECT_SCHEMAS=new Set(['ghgy-workspace-v4','ghgy-workspace-v5','ghgy-workspace-v6',SCHEMA_VERSION]);
const SUPPORTED_SCHEMA_VERSIONS=new Set(['ghgy-workspace-v1','ghgy-workspace-v2','ghgy-workspace-v3','ghgy-workspace-v4','ghgy-workspace-v5','ghgy-workspace-v6',SCHEMA_VERSION]);
const PACKAGE_LIMIT=256*1024*1024,RECOMMENDED_PART_LIMIT=200*1024*1024,FILE_LIMIT=128*1024*1024,ENTRY_LIMIT=1024,TOTAL_PACKAGE_LIMIT=2*1024*1024*1024,PART_LIMIT=16,TOTAL_ENTRY_LIMIT=16384,MEDIA_BLOB_LIMIT=32*1024*1024;
const CLASSIFICATION_TERMS_STORAGE_KEY='ghgy-custom-classification-terms-v1';
const WORKSPACE_CLEAR_CONFIRMATION='清空本机草稿';
const WORKSPACE_CLEAR_SIGNAL_KEY='ghgy-workspace-clear-signal-v1';
const WORKSPACE_MUTATION_SIGNAL_KEY='ghgy-workspace-mutation-signal-v1';
const WORKSPACE_DRAFT_META_KEYS=['last_backup_at','last_restore_at','last_saved_at','object_identity_state'];
const LEGACY_KEYS=['ghgy-local-derived-drafts-v1','yd-local-charts','ghgy-local-media-drafts-v1','yd-chart-notes','yd-custom-domains','yd-showcase','yd-showcase-layout-draft-v2','yd-showcase-layout-order-v1','yd-showcase-compositions-v1','yd-showcase-folders-v1','yd-chart-range-history','ghgy-chart-save-idempotency-v1','ghgy-display-name-overrides-v1','ghgy-chart-organization-overrides-v1',CLASSIFICATION_TERMS_STORAGE_KEY];
const HEAVY_COLLECTIONS={
  'yd-local-charts':{store:CHART_STORE,id:item=>String(item?.chart_id||''),updated:item=>String(item?.updated_at||''),imageFields:['png_url','canonical_png_url']},
  'ghgy-local-derived-drafts-v1':{store:DERIVED_STORE,id:item=>String(item?.indicator?.indicator_id||''),updated:item=>String(item?.definition?.updated_at||item?.indicator?.updated_at||'')},
  'ghgy-local-media-drafts-v1':{store:MEDIA_STORE,id:item=>String(item?.media_id||''),updated:item=>String(item?.updated_at||''),imageFields:['png_url'],nestedMedia:true},
};
const DURABLE_VALUE_KEYS=new Set(LEGACY_KEYS.filter(key=>!HEAVY_COLLECTIONS[key]));
const FORBIDDEN_KEY=/(?:^|_)(?:indicator_id|derived_id|chart_id|chart_version_id|composition_id|composition_version_id|current_version_id|folder_id|database_id|source_id|source_file_id|external_id|publisher|source_system|source_sheet|source_column|path|original_path|snapshot_path|internal_id)$/i;
const TECHNICAL_ID=/\b(?:PI|PC)-[A-F0-9]{12,}|\b(?:YD|SC|CH)-[A-Z0-9][A-Z0-9-]{3,}|\bLOCAL-(?:DERIVED|CH|MEDIA)-[A-Z0-9-]+/gi;
const EXECUTABLE_EXT=/\.(?:js|mjs|cjs|html?|svg|wasm|exe|dll|dylib|sh|command|bat|cmd|ps1|py|rb|php|jar)$/i;
const encoder=new TextEncoder(),decoder=new TextDecoder('utf-8',{fatal:true});let dbPromise=null,lastSaveError='',pendingImport=null,pendingClearImpact=null,workspaceMutationBlocked=false,workspaceClearInProgress=false,workspaceReadinessState='pending',workspaceReadinessMessage='内置正式内容可直接使用；正在检查此浏览器的“我的工作”…',restoreIndicatorRefs=new Map();const idbWriteQueues=new Map(),collectionCache=new Map(),storedRecordCache=new Map(),durableValueCache=new Map(),chartObjectURLs=new Map(),chartObjectURLSources=new Map();
const byId=id=>document.getElementById(id),nowISO=()=>new Date().toISOString(),clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
window.GHGY_WORKSPACE_READY_STATE=workspaceReadinessState;
function assertWorkspaceMutable(){if(workspaceReadinessState==='pending')throw new Error('本机工作区仍在检查，请稍后再保存；内置正式内容不受影响。');if(workspaceReadinessState==='error')throw new Error('本机工作区未能安全打开，已停止本机保存；内置正式内容仍可使用。');if(workspaceMutationBlocked)throw new Error('本机草稿已在另一个页面清空或正在重置，请刷新页面后再继续。')}
function compactLocalTimestamp(date=new Date()){const pad=value=>String(value).padStart(2,'0');return`${date.getFullYear()}${pad(date.getMonth()+1)}${pad(date.getDate())}-${pad(date.getHours())}${pad(date.getMinutes())}${pad(date.getSeconds())}`}
function safeJSON(text,fallback){try{return JSON.parse(text)}catch{return fallback}}
function cleanText(value){return String(value??'').replace(TECHNICAL_ID,'').replace(/\s*·\s*·/g,' · ').trim()}
function cleanClassificationName(value){return String(value??'').normalize('NFKC').replace(/\s+/g,' ').trim()}
function classificationNameKey(value){return cleanClassificationName(value).toLocaleLowerCase('zh-CN')}
function clientClassificationTermId(dimensionKey,termName){const text=`${dimensionKey}\0${classificationNameKey(termName)}`;let left=0x811c9dc5,right=0x9e3779b9;for(let index=0;index<text.length;index++){const code=text.charCodeAt(index);left=Math.imul(left^code,0x01000193)>>>0;right=Math.imul(right^(code+index),0x85ebca6b)>>>0}return`CLIENT-${dimensionKey}-${left.toString(16).padStart(8,'0')}${right.toString(16).padStart(8,'0')}`.toUpperCase()}
function classificationTermStore(){const value=safeJSON(workspaceGetItem(CLASSIFICATION_TERMS_STORAGE_KEY)||'{}',{});return value&&typeof value==='object'&&!Array.isArray(value)?value:{}}
function randomToken(prefix){const bytes=crypto.getRandomValues(new Uint8Array(16));return`${prefix}-${[...bytes].map(byte=>byte.toString(16).padStart(2,'0')).join('').toUpperCase()}`}
function localId(prefix){return`${prefix}-${Date.now()}-${crypto.getRandomValues(new Uint32Array(2)).join('-')}`}
function bytesToHex(bytes){return[...bytes].map(byte=>byte.toString(16).padStart(2,'0')).join('')}
async function sha256Bytes(bytes){return bytesToHex(new Uint8Array(await crypto.subtle.digest('SHA-256',bytes)))}
async function sha256JSON(value){return sha256Bytes(encoder.encode(stableJSON(value)))}
function stableJSON(value){if(Array.isArray(value))return`[${value.map(stableJSON).join(',')}]`;if(value&&typeof value==='object')return`{${Object.keys(value).sort().map(key=>`${JSON.stringify(key)}:${stableJSON(value[key])}`).join(',')}}`;return JSON.stringify(value??null)}
function assertObject(value,label){if(!value||typeof value!=='object'||Array.isArray(value))throw new Error(`${label}格式不正确。`)}
function assertKeys(value,allowed,label){assertObject(value,label);const unknown=Object.keys(value).filter(key=>!allowed.includes(key));if(unknown.length)throw new Error(`${label}包含未知字段：${unknown.slice(0,5).join('、')}`)}
function cachedCollection(key){return(collectionCache.get(key)||[]).slice()}
function workspaceGetItem(key){if(HEAVY_COLLECTIONS[key])return JSON.stringify(cachedCollection(key));if(DURABLE_VALUE_KEYS.has(key)&&durableValueCache.has(key))return durableValueCache.get(key);return localStorage.getItem(key)}
function parseArray(key){const value=safeJSON(workspaceGetItem(key)||'[]',[]);return Array.isArray(value)?value:[]}
function parseObject(key){const value=safeJSON(workspaceGetItem(key)||'{}',{});return value&&typeof value==='object'&&!Array.isArray(value)?value:{}}
function openDB(){if(!('indexedDB'in window))return Promise.reject(new Error('当前浏览器不支持 IndexedDB，不能可靠保存完整工作包。'));if(dbPromise)return dbPromise;dbPromise=new Promise((resolve,reject)=>{const request=indexedDB.open(DB_NAME,DB_VERSION);request.onupgradeneeded=()=>{const db=request.result;if(!db.objectStoreNames.contains(RECORD_STORE))db.createObjectStore(RECORD_STORE,{keyPath:'key'});if(!db.objectStoreNames.contains(META_STORE))db.createObjectStore(META_STORE,{keyPath:'key'});if(!db.objectStoreNames.contains(CHART_STORE))db.createObjectStore(CHART_STORE,{keyPath:'id'});if(!db.objectStoreNames.contains(DERIVED_STORE))db.createObjectStore(DERIVED_STORE,{keyPath:'id'});if(!db.objectStoreNames.contains(MEDIA_STORE))db.createObjectStore(MEDIA_STORE,{keyPath:'id'})};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error||new Error('IndexedDB 打开失败。'));request.onblocked=()=>reject(new Error('另一个旧页面阻止了存储升级，请关闭其他页面后重试。'))});return dbPromise}
async function idbGet(store,key){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readonly'),request=tx.objectStore(store).get(key);request.onsuccess=()=>resolve(request.result?.value);request.onerror=()=>reject(request.error)})}
async function idbGetRecord(store,key){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readonly'),request=tx.objectStore(store).get(key);request.onsuccess=()=>resolve(request.result||null);request.onerror=()=>reject(request.error)})}
async function idbGetAll(store){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readonly'),request=tx.objectStore(store).getAll();request.onsuccess=()=>resolve(request.result||[]);request.onerror=()=>reject(request.error)})}
async function idbPutRaw(store,record){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).put(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error||new Error('IndexedDB 写入被中止。'))})}
async function idbDeleteRaw(store,id){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).delete(id);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error||new Error('IndexedDB 删除被中止。'))})}
async function idbReplaceRaw(store,records){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite'),target=tx.objectStore(store);target.clear();for(const record of records)target.put(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error||new Error('IndexedDB 集合替换被中止。'))})}
async function restoreRawRecord(store,id,record){if(record)await idbPutRaw(store,record);else await idbDeleteRaw(store,id)}
function storageValueEqual(actual,expected){if(actual===expected)return true;try{return stableJSON(actual)===stableJSON(expected)}catch{return false}}
async function idbPut(store,key,value){const queueKey=`${store}/${key}`,previous=idbWriteQueues.get(queueKey)||Promise.resolve(),write=previous.catch(()=>{}).then(async()=>{const db=await openDB(),content_sha256=store===RECORD_STORE?await sha256JSON(value):null;await new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite'),record={key,value,updated_at:nowISO()};if(content_sha256)record.content_sha256=content_sha256;tx.objectStore(store).put(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error||new Error('IndexedDB 写入被中止。'))});if(!storageValueEqual(await idbGet(store,key),value))throw new Error('IndexedDB 写入校验失败。');if(content_sha256){const saved=await idbGetRecord(store,key);if(saved?.content_sha256!==content_sha256||await sha256JSON(saved.value)!==content_sha256)throw new Error('IndexedDB 内容哈希校验失败。')}});const tracked=write.finally(()=>{if(idbWriteQueues.get(queueKey)===tracked)idbWriteQueues.delete(queueKey)});idbWriteQueues.set(queueKey,tracked);return tracked}
async function validatedDurableValue(key){const record=await idbGetRecord(RECORD_STORE,key);if(!record)return null;if(record.content_sha256&&await sha256JSON(record.value)!==record.content_sha256)throw new Error(`浏览器客户内容完整性校验失败：${key}`);return record.value}
async function setMeta(key,value){return idbPut(META_STORE,key,JSON.stringify(value))}
async function getMeta(key,fallback=null){const text=await idbGet(META_STORE,key);return text==null?fallback:safeJSON(text,fallback)}
function dataURLBlob(value){const match=/^data:([^;,]+)?(;base64)?,(.*)$/s.exec(String(value||''));if(!match)return null;try{const binary=match[2]?atob(match[3]):decodeURIComponent(match[3]),bytes=new Uint8Array(binary.length);for(let index=0;index<binary.length;index++)bytes[index]=binary.charCodeAt(index);return new Blob([bytes],{type:match[1]||'application/octet-stream'})}catch{return null}}
function blobDataURL(blob){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result||''));reader.onerror=()=>reject(reader.error||new Error('图片快照读取失败。'));reader.readAsDataURL(blob)})}
async function blobDigest(blob){return sha256Bytes(new Uint8Array(await blob.arrayBuffer()))}
async function recordDigest(record){const image_hashes={};for(const[field,blob]of Object.entries(record.images||{}))image_hashes[field]=await blobDigest(blob);return sha256JSON({id:record.id,value:record.value,image_hashes})}
function recordSize(record){return encoder.encode(JSON.stringify(record.value||null)).length+Object.values(record.images||{}).reduce((sum,blob)=>sum+Number(blob?.size||0),0)}
function collectionDescriptor(key){const descriptor=HEAVY_COLLECTIONS[key];if(!descriptor)throw new Error(`未知大容量集合：${key}`);return descriptor}
function legacyRows(text){const rows=safeJSON(text||'[]',[]);return Array.isArray(rows)?rows:[]}
function mergeCollectionRows(key,...sources){const descriptor=collectionDescriptor(key),rows=new Map();for(const source of sources)for(const item of source||[]){const id=descriptor.id(item);if(!id)continue;const current=rows.get(id);if(!current||descriptor.updated(item)>=descriptor.updated(current))rows.set(id,item)}return[...rows.values()].sort((left,right)=>descriptor.id(left).localeCompare(descriptor.id(right)))}
function mediaPageGroups(value){const versions=Array.isArray(value.versions)?value.versions:[],groups=[];for(const[versionIndex,version]of versions.entries())groups.push({version,versionIndex,pages:version?.pages||[]});if(!versions.length)groups.push({version:null,versionIndex:-1,pages:value.pages||[]});return groups}
function mediaPageBlobKey(version,page,versionIndex,pageIndex){const versionKey=String(version?.local_version_id||version?.browser_version_key||`v${versionIndex+1}`).replace(/[^A-Za-z0-9_-]/g,'_'),pageKey=String(page?.local_page_id||`p${pageIndex+1}`).replace(/[^A-Za-z0-9_-]/g,'_');return`media/${versionKey}/${pageKey}`}
function nestedMediaBlob(source,id,existing,storedKey){if(typeof source==='string'&&source.startsWith('data:image/'))return dataURLBlob(source);if(typeof source==='string'&&source.startsWith('blob:')){const mapped=chartObjectURLSources.get(source);return mapped?.id===id?(existing?.images?.[mapped.field]||null):null}return storedKey?existing?.images?.[storedKey]||null:null}
async function extractNestedMediaBlobs(value,id,images,existing){for(const group of mediaPageGroups(value))for(const[pageIndex,page]of(group.pages||[]).entries()){if(!page||typeof page!=='object')continue;const base=mediaPageBlobKey(group.version,page,group.versionIndex,pageIndex),renditions=[{sources:['preview_png','png_url'],stored:'preview_blob_key',field:base,png:true,label:'TB 标准页面'},{sources:['hd_preview_png','hd_png_url'],stored:'hd_preview_blob_key',field:`${base}/hd_2x`,png:true,label:'TB 高清页面'},{sources:['source_data_url','source_url'],stored:'source_blob_key',field:`${base}/source`,png:false,label:'TB 原始图片'}];for(const rendition of renditions){const source=rendition.sources.map(key=>page[key]).find(Boolean),blob=nestedMediaBlob(source,id,existing,page[rendition.stored]);if(source&&!blob)throw new Error(`${rendition.label}引用已经失效，请重新打开后再保存。`);if(blob){if((rendition.png&&blob.type!=='image/png')||(!rendition.png&&!/^image\/(?:png|jpeg|webp)$/.test(blob.type))||blob.size>MEDIA_BLOB_LIMIT)throw new Error(`${rendition.label}格式无效或超过 32MB。`);images[rendition.field]=blob;page[rendition.stored]=rendition.field}for(const key of rendition.sources)delete page[key]}}if(Array.isArray(value.versions)&&value.versions.length)delete value.pages;for(const key of['preview_png','png_url','hd_preview_png','hd_png_url'])delete value[key];for(const[field,blob]of Object.entries(existing?.images||{}))if(field.startsWith('media/')&&!images[field])images[field]=blob}
async function prepareStoredRecord(key,item,existing=null){const descriptor=collectionDescriptor(key),id=descriptor.id(item);if(!id)throw new Error('草稿缺少稳定本机身份，已停止保存。');const value=clone(item),images={};if(descriptor.nestedMedia)await extractNestedMediaBlobs(value,id,images,existing);if(descriptor.imageFields?.length&&!descriptor.nestedMedia){for(const field of descriptor.imageFields){const source=value[field];if(typeof source==='string'&&source.startsWith('data:image/')){const blob=dataURLBlob(source);if(!blob)throw new Error('草稿图片快照无法解析，已停止保存。');images[field]=blob;delete value[field]}else if(typeof source==='string'&&source.startsWith('blob:')){const mapped=chartObjectURLSources.get(source),blob=mapped?.id===id?(existing?.images?.[mapped.field]||null):null;if(blob){images[field]=blob;delete value[field]}else throw new Error('草稿图片快照引用已经失效，请重新打开后再保存。')}else if(source==null)delete value[field]}for(const[field,blob]of Object.entries(existing?.images||{}))if(!images[field]&&value[field]===undefined)images[field]=blob}
  const record={id,value,images,updated_at:descriptor.updated(item)||nowISO()};record.byte_size=recordSize(record);record.content_sha256=await recordDigest(record);return record}
function revokeChartURLs(id){for(const[key,url]of chartObjectURLs)if(key.startsWith(`${id}/`)){URL.revokeObjectURL(url);chartObjectURLSources.delete(url);chartObjectURLs.delete(key)}}
async function hydrateStoredRecord(key,record,forExport=false){const value=clone(record.value),descriptor=collectionDescriptor(key);if(descriptor.imageFields?.length||descriptor.nestedMedia){if(!forExport)revokeChartURLs(record.id);const hydrated=new Map();for(const[field,blob]of Object.entries(record.images||{})){let source;if(forExport)source=await blobDataURL(blob);else{const cacheKey=`${record.id}/${field}/${record.content_sha256}`,url=URL.createObjectURL(blob);chartObjectURLs.set(cacheKey,url);chartObjectURLSources.set(url,{id:record.id,field});source=url}hydrated.set(field,source);if(!descriptor.nestedMedia)value[field]=source}if(descriptor.nestedMedia){for(const group of mediaPageGroups(value))for(const page of group.pages||[]){const standard=hydrated.get(page?.preview_blob_key),hd=hydrated.get(page?.hd_preview_blob_key),original=hydrated.get(page?.source_blob_key);if(standard){page.preview_png=standard;page.png_url=standard}if(hd){page.hd_preview_png=hd;page.hd_png_url=hd}if(original)page.source_data_url=original}const current=(value.versions||[]).find(version=>version.local_version_id&&version.local_version_id===value.current_version_id)||(value.versions||[]).find(version=>Number(version.version_no)===Number(value.current_version_no))||(value.versions||[]).at(-1);if(current?.pages?.length)value.pages=clone(current.pages);value.preview_png=value.pages?.[0]?.preview_png||null;value.png_url=value.preview_png;value.hd_preview_png=value.pages?.[0]?.hd_preview_png||null;value.hd_png_url=value.hd_preview_png}}return value}
async function verifyStoredRecord(record){const saved=await idbGetRecord(record.store||collectionDescriptor(record.collection_key).store,record.id);if(!saved||saved.content_sha256!==record.content_sha256||await recordDigest(saved)!==record.content_sha256)throw new Error('IndexedDB 草稿写入校验失败。');return saved}
async function ensureStorageHeadroom(additionalBytes){const estimate=await navigator.storage?.estimate?.();if(!estimate?.quota)return;const usage=Number(estimate.usage||0),quota=Number(estimate.quota||0),reserve=Math.max(50*1024*1024,quota*.1),needed=Math.max(0,Number(additionalBytes||0));if(usage+needed>quota-reserve)throw new Error('浏览器安全存储余量不足。现有草稿未被覆盖，请先导出完整工作包并清理不再需要的草稿。')}
async function saveCollectionItem(key,item,{skipQuota=false}={}){assertWorkspaceMutable();const descriptor=collectionDescriptor(key),id=descriptor.id(item),existing=storedRecordCache.get(`${key}/${id}`)||await idbGetRecord(descriptor.store,id),record=await prepareStoredRecord(key,item,existing);record.collection_key=key;record.store=descriptor.store;if(!skipQuota)await ensureStorageHeadroom(Math.max(0,record.byte_size-Number(existing?.byte_size||0)));const queueKey=`${descriptor.store}/${id}`,previous=idbWriteQueues.get(queueKey)||Promise.resolve(),write=previous.catch(()=>{}).then(async()=>{let verified;try{await idbPutRaw(descriptor.store,{...record,collection_key:undefined,store:undefined});verified=await verifyStoredRecord(record)}catch(error){await restoreRawRecord(descriptor.store,id,existing).catch(()=>{});throw error}storedRecordCache.set(`${key}/${id}`,verified);const hydrated=await hydrateStoredRecord(key,verified),rows=cachedCollection(key),index=rows.findIndex(row=>descriptor.id(row)===id);if(index>=0)rows[index]=hydrated;else rows.push(hydrated);rows.sort((left,right)=>descriptor.id(left).localeCompare(descriptor.id(right)));collectionCache.set(key,rows);lastSaveError='';await setMeta('last_saved_at',nowISO());updateStorageUI();return hydrated}).catch(error=>{lastSaveError=error?.message||String(error);updateStorageUI();window.dispatchEvent(new CustomEvent('ghgy-workspace-save-error',{detail:{key,error:lastSaveError}}));throw error});const tracked=write.finally(()=>{if(idbWriteQueues.get(queueKey)===tracked)idbWriteQueues.delete(queueKey)});idbWriteQueues.set(queueKey,tracked);return tracked}
async function deleteCollectionItem(key,id){assertWorkspaceMutable();const descriptor=collectionDescriptor(key),existing=storedRecordCache.get(`${key}/${id}`)||await idbGetRecord(descriptor.store,id);try{await idbDeleteRaw(descriptor.store,id);if(await idbGetRecord(descriptor.store,id))throw new Error('IndexedDB 草稿删除校验失败。')}catch(error){await restoreRawRecord(descriptor.store,id,existing).catch(()=>{});throw error}storedRecordCache.delete(`${key}/${id}`);if(descriptor.imageFields?.length)revokeChartURLs(id);collectionCache.set(key,cachedCollection(key).filter(item=>descriptor.id(item)!==id));await setMeta('last_saved_at',nowISO());updateStorageUI()}
function localMediaReferenceCount(value,id){let count=0;(function walk(item){if(Array.isArray(item))return item.forEach(walk);if(item&&typeof item==='object'){for(const[key,child]of Object.entries(item)){if(key===id||String(key).startsWith(`tb-local:${id}:`))count++;walk(child)}return}if(typeof item==='string'&&(item===id||item.startsWith(`tb-local:${id}:`)))count++})(value);return count}
function withoutLocalMediaReferences(value,id){if(Array.isArray(value))return value.map(item=>withoutLocalMediaReferences(item,id)).filter(item=>item!==undefined);if(value&&typeof value==='object'){if(value.local_media_id===id||String(value.asset_key||'').startsWith(`tb-local:${id}:`))return undefined;const result={};for(const[key,item]of Object.entries(value)){if(key===id||String(key).startsWith(`tb-local:${id}:`))continue;const cleaned=withoutLocalMediaReferences(item,id);if(cleaned!==undefined)result[key]=cleaned}return result}if(typeof value==='string'&&(value===id||value.startsWith(`tb-local:${id}:`)))return undefined;return value}
function localMediaDeleteImpact(id){const item=cachedCollection('ghgy-local-media-drafts-v1').find(row=>row.media_id===id);if(!/^LOCAL-MEDIA-[A-Z0-9-]+$/i.test(String(id||''))||!item||item.access_level!=='browser_local'||item.tb_id)throw new Error('只能永久删除当前浏览器自己创建的本机 TB；内置或正式 TB 受保护。');const references={};for(const key of DURABLE_VALUE_KEYS){const value=safeJSON(workspaceGetItem(key)||'null',null),count=localMediaReferenceCount(value,id);if(count)references[key]=count}return{id,title:item.title||'未命名素材',versions:(item.versions||[]).length||1,pages:(item.versions||[{pages:item.pages||[]}]).reduce((sum,version)=>sum+(version.pages||[]).length,0),references,reference_count:Object.values(references).reduce((sum,value)=>sum+value,0)}}
async function deleteLocalMedia(id,{cascade=false}={}){assertWorkspaceMutable();const impact=localMediaDeleteImpact(id);if(impact.reference_count&&!cascade){const error=new Error(`该本机 TB 仍被 ${impact.reference_count} 处展示组合或布局引用；请确认级联移除引用后再永久删除。`);error.impact=impact;throw error}await waitForWorkspaceWrites();const db=await openDB(),updates=[];if(cascade)for(const key of DURABLE_VALUE_KEYS){const raw=workspaceGetItem(key),value=safeJSON(raw||'null',null);if(!localMediaReferenceCount(value,id))continue;const text=JSON.stringify(withoutLocalMediaReferences(value,id)),record={key,value:text,updated_at:nowISO(),content_sha256:await sha256JSON(text)};updates.push(record)}await new Promise((resolve,reject)=>{const tx=db.transaction([MEDIA_STORE,RECORD_STORE],'readwrite');tx.objectStore(MEDIA_STORE).delete(id);for(const record of updates)tx.objectStore(RECORD_STORE).put(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error||new Error('本机 TB 永久删除事务失败。'));tx.onabort=()=>reject(tx.error||new Error('本机 TB 永久删除事务已中止。'))});if(await idbGetRecord(MEDIA_STORE,id))throw new Error('本机 TB 永久删除复核失败。');storedRecordCache.delete(`ghgy-local-media-drafts-v1/${id}`);revokeChartURLs(id);collectionCache.set('ghgy-local-media-drafts-v1',cachedCollection('ghgy-local-media-drafts-v1').filter(item=>item.media_id!==id));for(const record of updates)durableValueCache.set(record.key,record.value);await setMeta('last_saved_at',nowISO());localStorage.setItem(WORKSPACE_MUTATION_SIGNAL_KEY,`${nowISO()}:${crypto.getRandomValues(new Uint32Array(1))[0]}`);updateStorageUI();return impact}
async function replaceCollection(key,rows,{skipQuota=false,skipReadiness=false}={}){if(!skipReadiness)assertWorkspaceMutable();const descriptor=collectionDescriptor(key),clean=mergeCollectionRows(key,rows),prepared=[],previous=await idbGetAll(descriptor.store);for(const item of clean){const id=descriptor.id(item),existing=storedRecordCache.get(`${key}/${id}`)||previous.find(record=>record.id===id)||null;prepared.push(await prepareStoredRecord(key,item,existing))}if(!skipQuota){const existingSize=previous.reduce((sum,row)=>sum+Number(row.byte_size||0),0),nextSize=prepared.reduce((sum,row)=>sum+Number(row.byte_size||0),0);await ensureStorageHeadroom(Math.max(0,nextSize-existingSize))}let saved;try{await idbReplaceRaw(descriptor.store,prepared);saved=await idbGetAll(descriptor.store);if(saved.length!==prepared.length)throw new Error('IndexedDB 集合数量校验失败。');for(const record of saved)if(await recordDigest(record)!==record.content_sha256)throw new Error('IndexedDB 集合内容校验失败。')}catch(error){await idbReplaceRaw(descriptor.store,previous).catch(()=>{});throw error}for(const record of saved)storedRecordCache.set(`${key}/${record.id}`,record);if(descriptor.imageFields?.length)for(const item of cachedCollection(key))revokeChartURLs(descriptor.id(item));const hydrated=[];for(const record of saved.sort((a,b)=>a.id.localeCompare(b.id)))hydrated.push(await hydrateStoredRecord(key,record));collectionCache.set(key,hydrated);lastSaveError='';await setMeta('last_saved_at',nowISO());updateStorageUI();return cachedCollection(key)}
async function loadStoredCollection(key,forExport=false){const descriptor=collectionDescriptor(key),records=(await idbGetAll(descriptor.store)).sort((a,b)=>a.id.localeCompare(b.id)),rows=[];for(const record of records){if(!record.content_sha256||await recordDigest(record)!==record.content_sha256)throw new Error('浏览器草稿完整性校验失败；原始迁移备份仍保留，请先导出并联系维护人员。');storedRecordCache.set(`${key}/${record.id}`,record);rows.push(await hydrateStoredRecord(key,record,forExport))}if(!forExport)collectionCache.set(key,rows);return rows}
async function migrateHeavyCollections(){const marker=await getMeta('collection_storage_v2',null),counts={};if(marker?.version===2){for(const key of Object.keys(HEAVY_COLLECTIONS)){const legacyText=localStorage.getItem(key);if(legacyText!=null){const stored=await loadStoredCollection(key,true),merged=mergeCollectionRows(key,stored,legacyRows(legacyText));await replaceCollection(key,merged,{skipQuota:true,skipReadiness:true});localStorage.removeItem(key)}counts[key]=(await loadStoredCollection(key)).length}return counts}for(const key of Object.keys(HEAVY_COLLECTIONS)){const descriptor=collectionDescriptor(key),current=await idbGetAll(descriptor.store),durable=legacyRows(await idbGet(RECORD_STORE,key)),legacy=legacyRows(localStorage.getItem(key)),stored=[];for(const record of current)stored.push(await hydrateStoredRecord(key,record,true));const merged=mergeCollectionRows(key,durable,legacy,stored);await replaceCollection(key,merged,{skipQuota:true,skipReadiness:true});counts[key]=merged.length}await setMeta('collection_storage_v2',{version:2,verified_at:nowISO(),counts,legacy_backup_retained:true});for(const key of Object.keys(HEAVY_COLLECTIONS))localStorage.removeItem(key);return counts}
async function migrateLegacy(){const migrated=[],conflicts=[];for(const key of DURABLE_VALUE_KEYS){const record=await idbGetRecord(RECORD_STORE,key),durable=await validatedDurableValue(key),legacy=localStorage.getItem(key),chosen=legacy??durable;if(legacy!=null&&durable!=null&&!storageValueEqual(legacy,durable)){await idbPut(RECORD_STORE,`pre-indexeddb-only-backup/${key}/${compactLocalTimestamp()}`,durable);conflicts.push(key)}if(chosen!=null){if(!record?.content_sha256||!storageValueEqual(durable,chosen)){await idbPut(RECORD_STORE,key,chosen);migrated.push(key)}if(!storageValueEqual(await validatedDurableValue(key),chosen))throw new Error(`浏览器客户内容迁移校验失败：${key}`);durableValueCache.set(key,String(chosen));localStorage.removeItem(key)}else durableValueCache.set(key,null)}const counts=await migrateHeavyCollections();await setMeta('schema_version',SCHEMA_VERSION);await setMeta('legacy_migration',{verified_at:nowISO(),keys:migrated,conflicts,heavy_counts:counts,indexeddb_only:true,legacy_backup_retained:true});return migrated}
function durableSetItem(key,value){assertWorkspaceMutable();if(!DURABLE_VALUE_KEYS.has(key))return Promise.resolve();const text=String(value),previousCache=durableValueCache.has(key)?durableValueCache.get(key):localStorage.getItem(key),queueKey=`durable-value/${key}`,previous=idbWriteQueues.get(queueKey)||Promise.resolve();durableValueCache.set(key,text);const write=previous.catch(()=>{}).then(async()=>{let previousRecord=null,previousLoaded=false;try{previousRecord=await idbGetRecord(RECORD_STORE,key);previousLoaded=true;await ensureStorageHeadroom(Math.max(0,encoder.encode(text).length-encoder.encode(String(previousRecord?.value||'')).length));await idbPut(RECORD_STORE,key,text);if(!storageValueEqual(await validatedDurableValue(key),text))throw new Error('IndexedDB 客户内容写入校验失败。');localStorage.removeItem(key);lastSaveError='';await setMeta('last_saved_at',nowISO());updateStorageUI()}catch(error){if(previousLoaded)await restoreRawRecord(RECORD_STORE,key,previousRecord).catch(()=>{});if(durableValueCache.get(key)===text)durableValueCache.set(key,previousLoaded?(previousRecord?.value??null):previousCache);lastSaveError=error?.message||String(error);updateStorageUI();window.dispatchEvent(new CustomEvent('ghgy-workspace-save-error',{detail:{key,error:lastSaveError}}));throw error}});const tracked=write.finally(()=>{if(idbWriteQueues.get(queueKey)===tracked)idbWriteQueues.delete(queueKey)});idbWriteQueues.set(queueKey,tracked);return tracked}
function workspaceSetItem(key,value){assertWorkspaceMutable();if(HEAVY_COLLECTIONS[key])return replaceCollection(key,legacyRows(value));if(DURABLE_VALUE_KEYS.has(key))return durableSetItem(key,value);localStorage.setItem(key,value);return Promise.resolve()}
window.workspaceSetItem=workspaceSetItem;
window.workspaceGetItem=workspaceGetItem;
async function snapshotImage(url){const response=await fetch(url,{cache:'no-store'});if(!response.ok)throw new Error(`图片快照读取失败（${response.status}）`);const blob=await response.blob();if(!blob.type.startsWith('image/'))throw new Error('图片快照格式无效。');return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result||''));reader.onerror=()=>reject(reader.error||new Error('图片快照读取失败。'));reader.readAsDataURL(blob)})}

function publicIndicators(){return clone(window.GALLERY_BOOTSTRAP?.indicators||window.state?.indicators||[])}
function publicCharts(){return clone(window.GALLERY_BOOTSTRAP?.charts||window.state?.charts||[]).filter(item=>item?.access_level==='public'||window.GALLERY_BOOTSTRAP)}
function safeDescriptor(meta){return{kind:'public_dependency',feedback_ref:cleanText(meta?.feedback_ref),name:cleanText(meta?.name),unit:cleanText(meta?.unit),frequency:cleanText(meta?.frequency),domain:cleanText(meta?.domain),subdomain:cleanText(meta?.subdomain)}}
function descriptorKey(value){return[value?.name,value?.unit,value?.frequency,value?.domain,value?.subdomain].map(item=>String(item||'').trim()).join('\u001f')}
function dependencyFor(indicatorId,objectRefByIndicatorId){if(objectRefByIndicatorId.has(indicatorId))return{kind:'object_dependency',object_ref:objectRefByIndicatorId.get(indicatorId)};const meta=publicIndicators().find(item=>item.indicator_id===indicatorId);if(!meta?.feedback_ref)throw new Error('草稿引用的正式指标缺少稳定回传身份，不能导出。');return safeDescriptor(meta)}
function scrubGeneric(value){if(Array.isArray(value))return value.map(scrubGeneric);if(value&&typeof value==='object'){const result={};for(const[key,item]of Object.entries(value)){if(FORBIDDEN_KEY.test(key)||['chart_id','derived_id','indicator_id','composition_id','folder_id'].includes(key))continue;result[key]=scrubGeneric(item)}return result}return typeof value==='string'?cleanText(value):value}
function exportConfig(config,objectRefByIndicatorId){const safe=scrubGeneric(config||{}),instanceMap=new Map();safe.series=(config?.series||[]).map((series,index)=>{const instanceKey=`series-${index+1}`;if(series.instance_id)instanceMap.set(series.instance_id,instanceKey);return{...scrubGeneric(series),series_key:instanceKey,dependency:dependencyFor(series.indicator_id,objectRefByIndicatorId)}});if(safe.time_alignment){safe.time_alignment.reference_series_key=instanceMap.get(config?.time_alignment?.reference_instance_id)||null;delete safe.time_alignment.reference_instance_id}return safe}
function mappedExportReference(value,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs=new Map()){return chartRefs.get(value)||folderRefs.get(value)||indicatorRefs.get(value)||publicChartRefs.get(value)||publicIndicatorRefs.get(value)||mediaPageRefs.get(value)||null}
function remapExportTree(value,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs=new Map()){if(Array.isArray(value))return value.map(item=>remapExportTree(item,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs));if(value&&typeof value==='object'){const result={};for(const[key,item]of Object.entries(value)){if(['composition_id','chart_version_id','composition_version_id','current_version_id'].includes(key))continue;if(key==='chart_id'){result.chart_ref=chartRefs.get(item)||publicChartRefs.get(item)||null;continue}if(key==='folder_id'){result.folder_ref=folderRefs.get(item)||null;continue}if(key==='indicator_id'){result.indicator_ref=indicatorRefs.get(item)||publicIndicatorRefs.get(item)||null;continue}const mappedKey=mappedExportReference(key,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs);if(!mappedKey&&/^tb-local:LOCAL-MEDIA-/i.test(key))throw new Error(`TB 页面引用缺少工作包素材映射：${key}`);result[mappedKey||key]=remapExportTree(item,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs)}return result}if(typeof value==='string'){const mapped=mappedExportReference(value,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs);if(!mapped&&/^tb-local:LOCAL-MEDIA-/i.test(value))throw new Error(`TB 页面引用缺少工作包素材映射：${value}`);return mapped||scrubGeneric(value)}return value}
function portableCompositionMedia(value,mediaRefs,portableMedia){const copy=clone(value),convert=version=>{if(!Array.isArray(version?.items))return;version.items=version.items.map(item=>{if(item?.asset_type!=='tb'||!String(item.asset_key||'').startsWith('tb-local:'))return item;const match=String(item.asset_key).match(/^tb-local:(LOCAL-MEDIA-[A-Z0-9-]+):(\d+)$/i),mediaId=match?.[1],mediaRef=mediaId&&mediaRefs.get(mediaId),mapping=mediaId&&portableMedia.get(mediaId);if(!mediaRef||!mapping)throw new Error(`组合中的本机 TB 页面缺少稳定素材依赖：${item.asset_key}`);const versionMap=mapping.byVersionId.get(item.local_version_id)||mapping.byVersionKey.get(item.browser_version_key)||mapping.current,pageNo=Number(item.tb_page_no||item.page_no||match[2]),pageMap=versionMap?.byPageId.get(item.local_page_id)||versionMap?.byPageNo.get(pageNo);if(!versionMap||!pageMap)throw new Error(`组合中的本机 TB 页面缺少精确版本或页面映射：${item.asset_key}`);const restored={...item,asset_key:`media-page:${mediaRef}:${pageNo}`,media_ref:mediaRef,media_page_no:pageNo,media_version_ref:versionMap.version_ref,media_page_ref:pageMap.page_ref};for(const key of['local_media_id','local_version_id','local_page_id','tb_id','tb_version_id','tb_page_id','browser_version_key'])delete restored[key];return restored})};convert(copy);for(const version of copy.versions||[])convert(version);return copy}
function mediaDependencyFromPageReference(value){const match=String(value||'').match(/^media-page:(WO-[A-Z0-9]{16,64}):\d+$/);return match?.[1]||null}
function orderedUnique(values){return[...new Set((values||[]).map(value=>String(value||'')).filter(Boolean))]}
function currentCompositionPayload(payload){const versions=Array.isArray(payload?.versions)?payload.versions:[],currentNo=Number(payload?.current_version_no||0);return versions.find(item=>Number(item?.version_no||0)===currentNo)||versions.at(-1)||payload||{}}
function compositionChartRefs(payload){return orderedUnique((currentCompositionPayload(payload).items||[]).map(item=>item?.chart_ref||item?.asset_key))}
function showcaseChartRefs(payload){return orderedUnique([...(payload?.order||[]),...(payload?.selected||[])])}
function hasMeaningfulLayout(value){if(Array.isArray(value))return value.some(hasMeaningfulLayout);if(value&&typeof value==='object')return Object.values(value).some(hasMeaningfulLayout);return ![null,undefined,'',false,0].includes(value)}
function redundantCompositionEnvelope(showcasePayload,envelopes){const references=showcaseChartRefs(showcasePayload);if(!references.length)return null;const matches=envelopes.filter(envelope=>{const payload=envelope.payload||{},current=currentCompositionPayload(payload),layout=current.layout||payload.layout||{};return stableJSON(compositionChartRefs(payload))===stableJSON(references)&&(!hasMeaningfulLayout(showcasePayload.layout||{})||stableJSON(showcasePayload.layout||{})===stableJSON(layout))});return matches.length===1?matches[0]:null}
function collectDependencies(payload){const found=new Set();(function walk(value){if(Array.isArray(value))return value.forEach(walk);if(value&&typeof value==='object')return Object.values(value).forEach(walk);if(typeof value==='string'&&/^(?:WO-[A-Z0-9]{16,64}|F(?:IR|CR)-[A-F0-9]{20})$/.test(value))found.add(value)})(payload);return[...found].sort()}
function containsTechnicalId(value){TECHNICAL_ID.lastIndex=0;const found=TECHNICAL_ID.test(String(value??''));TECHNICAL_ID.lastIndex=0;return found}
function technicalReferencePaths(value){const found=[];(function walk(item,path){if(found.length>=5)return;if(Array.isArray(item)){item.forEach((child,index)=>walk(child,`${path}[${index}]`));return}if(item&&typeof item==='object'){for(const[key,child]of Object.entries(item)){if(containsTechnicalId(key))found.push(`${path}.<关联键>`);walk(child,`${path}.${key}`)}return}if(typeof item==='string'&&containsTechnicalId(item))found.push(path)})(value,'objects');return[...new Set(found)]}
async function canonicalMediaPNG(source,label,expectedWidth=1550,expectedHeight=700){const blob=dataURLBlob(source);if(!blob||blob.type!=='image/png')throw new Error(`${label}不是规范 PNG。`);if(blob.size>MEDIA_BLOB_LIMIT)throw new Error(`${label}超过 32MB。`);const bytes=new Uint8Array(await blob.arrayBuffer());if(bytes.length<24||bytes[0]!==137||bytes[1]!==80||bytes[2]!==78||bytes[3]!==71||bytes[12]!==73||bytes[13]!==72||bytes[14]!==68||bytes[15]!==82)throw new Error(`${label}的 PNG 头损坏。`);const view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength),width=view.getUint32(16),height=view.getUint32(20);if(width!==expectedWidth||height!==expectedHeight)throw new Error(`${label}必须是 ${expectedWidth}×${expectedHeight} PNG。`);const sha256=await sha256Bytes(bytes);return{data:bytes,size:bytes.length,sha256,width,height,path:`blobs/sha256/${sha256.slice(0,2)}/${sha256}.png`}}
function localMediaVersions(media){if(Array.isArray(media.versions)&&media.versions.length)return media.versions;return[{local_version_id:media.current_version_id||`legacy:${media.media_id}:v1`,browser_version_key:media.browser_version_key||`legacy:${media.media_id}:v1`,version_no:1,title:media.title,config:clone(media.config||{}),pages:(media.pages||[{preview_png:media.preview_png||media.png_url}]).map((page,index)=>({...clone(page),local_page_id:page.local_page_id||`legacy:${media.media_id}:p${index+1}`,page_no:index+1})),created_at:media.created_at||media.updated_at||nowISO()}]}
async function identityContext(){const saved=await getMeta('object_identity_state',{workspace_id:null,objects:{},last_snapshot_id:null});saved.workspace_id=/^GW-[A-Z0-9]{16,64}$/.test(saved.workspace_id||'')?saved.workspace_id:randomToken('GW');saved.objects=saved.objects||{};saved.media_versions=saved.media_versions||{};saved.media_pages=saved.media_pages||{};const objectId=(type,key)=>{const identityKey=`${type}:${key}`;if(!saved.objects[identityKey]?.object_id)saved.objects[identityKey]={object_id:randomToken('WO'),revision:0,content_sha256:null};return saved.objects[identityKey].object_id},portableRef=(kind,key)=>{const target=kind==='version'?saved.media_versions:saved.media_pages,prefix=kind==='version'?'WV':'WP',stableKey=String(key||'');if(!stableKey)throw new Error(`TB ${kind==='version'?'版本':'页面'}缺少稳定本机身份。`);if(!target[stableKey])target[stableKey]=randomToken(prefix);return target[stableKey]};const envelope=async(type,key,payload,lifecycle='active')=>{const identityKey=`${type}:${key}`,current=saved.objects[identityKey]||{object_id:objectId(type,key),revision:0,content_sha256:null},hash=await sha256JSON(payload),revision=current.content_sha256===hash?Math.max(1,Number(current.revision||1)):Number(current.revision||0)+1;saved.objects[identityKey]={object_id:current.object_id,revision,content_sha256:hash};return{object_id:current.object_id,object_type:type,revision,content_sha256:hash,lifecycle_status:lifecycle||'active',dependencies:collectDependencies(payload),payload}};return{saved,objectId,portableRef,envelope}}

async function buildWorkspaceModel(exportMode='full'){await window.GHGY_WORKSPACE_READY;const identity=await identityContext(),rawDerived=cachedCollection('ghgy-local-derived-drafts-v1'),rawCharts=(await loadStoredCollection('yd-local-charts',true)).filter(chart=>String(chart?.chart_id||'').startsWith('LOCAL-CH-')),rawMedia=(await loadStoredCollection('ghgy-local-media-drafts-v1',true)).filter(item=>String(item?.media_id||'').startsWith('LOCAL-MEDIA-')),rawFolders=parseArray('yd-showcase-folders-v1'),rawCompositions=parseArray('yd-showcase-compositions-v1'),rawNotes=parseObject('yd-chart-notes'),rawOrganizationOverrides=parseObject('ghgy-chart-organization-overrides-v1'),rangeHistory=parseArray('yd-chart-range-history');if(exportMode==='incremental'&&(!identity.saved.last_snapshot_id||!identity.saved.last_snapshot_digest))throw new Error('尚无上次成功打包基线，请先使用“导出全部工作”。');const indicatorRefs=new Map(rawDerived.map(draft=>[draft?.indicator?.indicator_id,identity.objectId('indicator',draft?.indicator?.indicator_id||randomToken('LEGACY'))])),chartRefs=new Map(rawCharts.map(chart=>[chart.chart_id,identity.objectId('chart',chart.chart_id)])),mediaRefs=new Map(rawMedia.map(media=>[media.media_id,identity.objectId('media',media.media_id)])),mediaPageRefs=new Map(rawMedia.flatMap(media=>{const pageCount=Math.max(1,Array.isArray(media.pages)?media.pages.length:1),objectRef=mediaRefs.get(media.media_id);return Array.from({length:pageCount},(_,index)=>[`tb-local:${media.media_id}:${index+1}`,`media-page:${objectRef}:${index+1}`])})),folderRefs=new Map(rawFolders.map(folder=>[folder.folder_id,identity.objectId('folder',folder.folder_id)])),publicChartRefs=new Map(publicCharts().filter(chart=>chart?.chart_id&&chart?.feedback_ref).map(chart=>[chart.chart_id,chart.feedback_ref])),publicIndicatorRefs=new Map(publicIndicators().filter(indicator=>indicator?.indicator_id&&indicator?.feedback_ref).map(indicator=>[indicator.indicator_id,indicator.feedback_ref])),objects=[],compositionEnvelopes=[];
  const binaryEntries=[],binaryPaths=new Set(),portableMedia=new Map();
  for(const draft of rawDerived){const definition=draft?.definition||{},config=definition.config||{},payload={metadata:scrubGeneric(draft?.indicator||{}),definition:{name:cleanText(definition.name),operation:definition.operation,expression:cleanText(definition.expression),unit:cleanText(definition.unit),frequency:cleanText(definition.frequency),domain:cleanText(definition.domain),status:'browser_draft',created_at:definition.created_at||null,manual_activity_at:definition.manual_activity_at||definition.updated_at||null,updated_at:definition.updated_at||null,config:{...scrubGeneric(config),inputs:(config.inputs||[]).map(input=>({...scrubGeneric(input),dependency:dependencyFor(input.indicator_id,indicatorRefs)}))}},observations:(draft?.series||[]).map(point=>({date:String(point.date||''),value:Number(point.value)})).filter(point=>/^\d{4}-\d{2}-\d{2}$/.test(point.date)&&Number.isFinite(point.value))};objects.push(await identity.envelope('indicator',draft?.indicator?.indicator_id,payload))}
  for(const chart of rawCharts){const chartRef=chartRefs.get(chart.chart_id),payload={title:cleanText(chart.title),domain:cleanText(chart.domain),created_at:chart.created_at||null,manual_activity_at:chart.manual_activity_at||chart.updated_at||null,updated_at:chart.updated_at||null,config:exportConfig(chart.config||{},indicatorRefs),preview_png:typeof chart.png_url==='string'&&chart.png_url.startsWith('data:image/png;base64,')?chart.png_url:null,figure:scrubGeneric(chart.interactive_figure||chart.figure||null),render:{fingerprint:cleanText(chart.render_fingerprint),data_snapshot_hash:cleanText(chart.data_snapshot_hash),renderer_version:cleanText(chart.renderer_version)},note:{text:cleanText(rawNotes[chart.chart_id]?.note_text||chart.note_text||''),version:Number(rawNotes[chart.chart_id]?.version||chart.note_version||0)},version_history:scrubGeneric(rangeHistory.filter(item=>item.chart_id===chart.chart_id))};objects.push(await identity.envelope('chart',chart.chart_id,payload,chart.lifecycle_status||'active'));const organization=chart.organization||{},organizationPayload={chart_ref:chartRef,is_pinned:Boolean(organization.is_pinned),pin_rank:organization.pin_rank??null,classifications:Object.fromEntries(Object.entries(organization.classifications||{}).map(([key,terms])=>[key,(terms||[]).map(term=>cleanText(term.term_name||term)).filter(Boolean)]))};objects.push(await identity.envelope('chart_organization',chart.chart_id,organizationPayload))}
  for(const media of rawMedia){
    const assetKind=(media.asset_kind||media.media_kind)==='table'?'table':'screenshot',versions=[],mapping={byVersionId:new Map(),byVersionKey:new Map(),current:null};
    for(const[sourceIndex,sourceVersion]of localMediaVersions(media).entries()){
      const localVersionId=sourceVersion.local_version_id||`legacy:${media.media_id}:v${sourceIndex+1}`,versionRef=identity.portableRef('version',localVersionId),pages=[],versionMap={version_ref:versionRef,byPageId:new Map(),byPageNo:new Map()};
      for(const[pageIndex,sourcePage]of(sourceVersion.pages||[]).entries()){
        const localPageId=sourcePage.local_page_id||`legacy:${localVersionId}:p${pageIndex+1}`,pageRef=identity.portableRef('page',localPageId),label=`${media.title||'未命名素材'} · V${sourceIndex+1} · 第 ${pageIndex+1} 页`,png=await canonicalMediaPNG(sourcePage.preview_png||sourcePage.png_url,label),descriptor={page_ref:pageRef,page_no:pageIndex+1,blob_path:png.path,blob_sha256:png.sha256,byte_size:png.size,mime_type:'image/png',width:png.width,height:png.height,config:scrubGeneric(sourcePage.config||{})},hdSource=sourcePage.hd_preview_png||sourcePage.hd_png_url;if(hdSource){const hd=await canonicalMediaPNG(hdSource,`${label}高清版`,3100,1400);Object.assign(descriptor,{hd_blob_path:hd.path,hd_blob_sha256:hd.sha256,hd_byte_size:hd.size,hd_mime_type:'image/png',hd_width:hd.width,hd_height:hd.height});if(!binaryPaths.has(hd.path)){binaryPaths.add(hd.path);binaryEntries.push({path:hd.path,data:hd.data,size:hd.size,sha256:hd.sha256})}}pages.push(descriptor);versionMap.byPageId.set(localPageId,descriptor);versionMap.byPageNo.set(pageIndex+1,descriptor);if(!binaryPaths.has(png.path)){binaryPaths.add(png.path);binaryEntries.push({path:png.path,data:png.data,size:png.size,sha256:png.sha256})}
      }
      if(!pages.length)throw new Error(`表格或图片存在缺页版本：${media.title||'未命名素材'} · V${sourceIndex+1}`);
      const portableVersion={version_ref:versionRef,version_no:sourceIndex+1,title:cleanText(sourceVersion.title||media.title),created_at:sourceVersion.created_at||media.created_at||null,config:scrubGeneric(sourceVersion.config||media.config||{}),pages};versions.push(portableVersion);mapping.byVersionId.set(localVersionId,versionMap);mapping.byVersionKey.set(sourceVersion.browser_version_key||localVersionId,versionMap);mapping.current=versionMap
    }
    portableMedia.set(media.media_id,mapping);const payload={title:cleanText(media.title),asset_kind:assetKind,media_kind:assetKind==='screenshot'?'image':'table',requested_access_level:['public','chart_only','private','restricted'].includes(media.requested_access_level)?media.requested_access_level:'private',created_at:media.created_at||null,updated_at:media.updated_at||null,current_version_ref:versions.at(-1).version_ref,versions,canvas:{width:1550,height:700,ratio:'15.5:7'}};objects.push(await identity.envelope('media',media.media_id,payload,media.lifecycle_status||'active'));
    const organizationPayload={media_ref:mediaRefs.get(media.media_id),is_pinned:Boolean(media.is_pinned),pin_rank:media.pin_rank??null,classifications:Object.fromEntries(Object.entries(media.classifications||{}).map(([key,terms])=>[key,(terms||[]).map(term=>cleanText(term.term_name||term)).filter(Boolean)]))};objects.push(await identity.envelope('media_organization',media.media_id,organizationPayload,media.lifecycle_status||'active'))
  }
  for(const[feedbackRef,organization]of Object.entries(rawOrganizationOverrides)){if(!/^FCR-[A-F0-9]{20}$/.test(feedbackRef)||!publicCharts().some(chart=>chart.feedback_ref===feedbackRef))continue;const organizationPayload={chart_ref:feedbackRef,is_pinned:Boolean(organization?.is_pinned),pin_rank:organization?.pin_rank??null,classifications:Object.fromEntries(Object.entries(organization?.classifications||{}).map(([key,terms])=>[key,(terms||[]).map(term=>cleanText(term.term_name||term)).filter(Boolean)]))};objects.push(await identity.envelope('chart_organization',`public:${feedbackRef}`,organizationPayload))}
  for(const folder of rawFolders){const payload={folder_name:cleanText(folder.folder_name||folder.name),sort_order:Number(folder.sort_order||0),is_pinned:Boolean(folder.is_pinned),pin_rank:folder.pin_rank??null,access_level:'browser_local',lifecycle_status:folder.lifecycle_status||'active',trashed_at:folder.trashed_at||null,updated_at:folder.updated_at||null};objects.push(await identity.envelope('folder',folder.folder_id,payload,payload.lifecycle_status))}
  for(const composition of rawCompositions){const portable=portableCompositionMedia(composition,mediaRefs,portableMedia),payload=scrubGeneric(remapExportTree(portable,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs));payload.composition_name=cleanText(composition.composition_name);payload.folder_ref=folderRefs.get(composition.folder_id)||null;const envelope=await identity.envelope('composition',composition.composition_id,payload,composition.lifecycle_status||'active');objects.push(envelope);compositionEnvelopes.push(envelope)}
  const showcaseSource={selected:parseArray('yd-showcase'),order:parseArray('yd-showcase-layout-order-v1'),layout:parseObject('yd-showcase-layout-draft-v2')},showcasePayload=scrubGeneric(remapExportTree(showcaseSource,chartRefs,folderRefs,indicatorRefs,publicChartRefs,publicIndicatorRefs,mediaPageRefs));showcasePayload.media_refs=orderedUnique([...(showcasePayload.selected||[]),...(showcasePayload.order||[])].map(mediaDependencyFromPageReference));const sourceComposition=redundantCompositionEnvelope(showcasePayload,compositionEnvelopes);if(sourceComposition){showcasePayload.transient_independent=false;showcasePayload.source_composition_ref=sourceComposition.object_id}objects.push(await identity.envelope('showcase_layout','singleton',showcasePayload));
  const savedNames=parseObject('ghgy-display-name-overrides-v1');for(const[entityType,records]of Object.entries({indicator:savedNames.indicator||{},chart:savedNames.chart||{}})){const rows=entityType==='indicator'?publicIndicators():publicCharts();for(const[entityId,record]of Object.entries(records)){const item=rows.find(row=>(entityType==='indicator'?row.indicator_id:row.chart_id)===entityId),feedbackRef=item?.feedback_ref,displayName=cleanText(record?.display_name);if(!feedbackRef||!displayName)continue;const original=cleanText(entityType==='indicator'?(item.original_name||item.published_name||item.name):(item.original_title||item.published_title||item.title)),payload={entity_type:entityType,feedback_ref:feedbackRef,original_name:original,github_name:original,customer_name:displayName,updated_at:record.updated_at||null};objects.push(await identity.envelope('rename',`${entityType}:${feedbackRef}`,payload))}}
  const customClassificationTerms=classificationTermStore(),preferences={custom_domains:parseArray('yd-custom-domains').map(cleanText).filter(Boolean),custom_classification_terms:Object.fromEntries(Object.entries(customClassificationTerms).map(([key,terms])=>[key,(Array.isArray(terms)?terms:[]).map(term=>cleanClassificationName(term?.term_name||term)).filter(Boolean)])),capacity_targets:{indicators:100,charts:300,media:500,notes:300,compositions:100,folders:100,organization_relations:10000}};objects.push(await identity.envelope('workspace_preferences','singleton',preferences));
  objects.sort((a,b)=>`${a.object_type}/${a.object_id}`.localeCompare(`${b.object_type}/${b.object_id}`));
  const allObjects=objects.slice(),previous=identity.saved.last_snapshot_objects||{};
  if(exportMode==='incremental'){const currentIds=new Set(allObjects.map(item=>item.object_id));for(const prior of Object.values(previous))if(prior?.object_id&&!currentIds.has(prior.object_id))objects.push({...clone(prior),revision:Number(prior.revision||0)+1,lifecycle_status:'delete_requested'});const changed=objects.filter(item=>!previous[item.object_id]||previous[item.object_id].content_sha256!==item.content_sha256||previous[item.object_id].lifecycle_status!==item.lifecycle_status);objects.splice(0,objects.length,...changed.sort((a,b)=>`${a.object_type}/${a.object_id}`.localeCompare(`${b.object_type}/${b.object_id}`)))}
  const generatedAt=nowISO(),packageId=randomToken('GP'),snapshotId=randomToken('GS'),warnings=[],counts={objects:objects.length,indicators:objects.filter(item=>item.object_type==='indicator').length,charts:objects.filter(item=>item.object_type==='chart').length,media:objects.filter(item=>item.object_type==='media').length,notes:objects.filter(item=>item.object_type==='chart'&&item.payload.note?.text).length,compositions:objects.filter(item=>item.object_type==='composition').length,folders:objects.filter(item=>item.object_type==='folder').length,renames:objects.filter(item=>item.object_type==='rename').length,organization_relations:objects.filter(item=>['chart_organization','media_organization'].includes(item.object_type)).reduce((sum,item)=>sum+Object.values(item.payload.classifications||{}).reduce((count,terms)=>count+(terms||[]).length,0),0)};
  for(const[key,target]of Object.entries({indicators:100,charts:300,media:500,notes:300,compositions:100,folders:100,organization_relations:10000}))if(counts[key]>target)warnings.push(`${key} 数量 ${counts[key]} 已超过建议目标 ${target}`);
  const unsafePaths=technicalReferencePaths(objects);if(unsafePaths.length)throw new Error(`工作包脱敏检查失败：仍包含内部或运行时关联键（位置：${unsafePaths.join('、')}）。`);
  const requiredBlobs=new Set(objects.filter(item=>item.object_type==='media'&&item.lifecycle_status!=='delete_requested').flatMap(item=>(item.payload.versions||[]).flatMap(version=>(version.pages||[]).map(page=>page.blob_path)))),selectedBinaries=binaryEntries.filter(entry=>requiredBlobs.has(entry.path));
  return{schema_version:SCHEMA_VERSION,export_mode:exportMode,workspace_id:identity.saved.workspace_id,package_id:packageId,snapshot_id:snapshotId,base_snapshot_id:exportMode==='incremental'?identity.saved.last_snapshot_id:null,base_content_digest:exportMode==='incremental'?identity.saved.last_snapshot_digest:null,generated_at:generatedAt,objects,all_objects:allObjects,binary_entries:selectedBinaries,counts,warnings,identity_state:identity.saved}
}

const CRC_TABLE=(()=>{const table=new Uint32Array(256);for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=(c&1)?0xedb88320^(c>>>1):c>>>1;table[n]=c>>>0}return table})();
function crc32(bytes){let c=0xffffffff;for(const byte of bytes)c=CRC_TABLE[(c^byte)&0xff]^(c>>>8);return(c^0xffffffff)>>>0}
function dosDateTime(date=new Date()){let year=Math.max(1980,date.getFullYear());return{date:((year-1980)<<9)|((date.getMonth()+1)<<5)|date.getDate(),time:(date.getHours()<<11)|(date.getMinutes()<<5)|Math.floor(date.getSeconds()/2)}}
function zipStore(files){const chunks=[],central=[];let offset=0;const stamp=dosDateTime();for(const file of files){const name=encoder.encode(file.name),data=file.data,crc=crc32(data),header=new Uint8Array(30+name.length),view=new DataView(header.buffer);view.setUint32(0,0x04034b50,true);view.setUint16(4,20,true);view.setUint16(6,0x0800,true);view.setUint16(8,0,true);view.setUint16(10,stamp.time,true);view.setUint16(12,stamp.date,true);view.setUint32(14,crc,true);view.setUint32(18,data.length,true);view.setUint32(22,data.length,true);view.setUint16(26,name.length,true);header.set(name,30);chunks.push(header,data);const entry=new Uint8Array(46+name.length),ev=new DataView(entry.buffer);ev.setUint32(0,0x02014b50,true);ev.setUint16(4,20,true);ev.setUint16(6,20,true);ev.setUint16(8,0x0800,true);ev.setUint16(10,0,true);ev.setUint16(12,stamp.time,true);ev.setUint16(14,stamp.date,true);ev.setUint32(16,crc,true);ev.setUint32(20,data.length,true);ev.setUint32(24,data.length,true);ev.setUint16(28,name.length,true);ev.setUint32(38,0,true);ev.setUint32(42,offset,true);entry.set(name,46);central.push(entry);offset+=header.length+data.length}const centralOffset=offset,centralSize=central.reduce((sum,item)=>sum+item.length,0),end=new Uint8Array(22),ev=new DataView(end.buffer);ev.setUint32(0,0x06054b50,true);ev.setUint16(8,files.length,true);ev.setUint16(10,files.length,true);ev.setUint32(12,centralSize,true);ev.setUint32(16,centralOffset,true);return new Blob([...chunks,...central,end],{type:'application/zip'})}
function validPackagePath(name){return Boolean(name)&&name.length<=240&&!name.includes('\\')&&!name.startsWith('/')&&!name.split('/').includes('..')&&!name.includes('\0')&&!EXECUTABLE_EXT.test(name)}
async function parseZip(file){if(!file||file.size>PACKAGE_LIMIT)throw new Error('单个工作包分卷超过256MB上限。');const bytes=new Uint8Array(await file.arrayBuffer()),view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);let eocd=-1;for(let offset=bytes.length-22;offset>=Math.max(0,bytes.length-65557);offset--)if(view.getUint32(offset,true)===0x06054b50){eocd=offset;break}if(eocd<0)throw new Error('ZIP中央目录缺失或文件已损坏。');const count=view.getUint16(eocd+10,true),centralSize=view.getUint32(eocd+12,true),centralOffset=view.getUint32(eocd+16,true);if(!count||count>ENTRY_LIMIT)throw new Error(`工作包文件数量必须在1到${ENTRY_LIMIT}之间。`);if(centralOffset+centralSize>eocd)throw new Error('ZIP中央目录越界。');const result=new Map();let cursor=centralOffset,total=0;for(let index=0;index<count;index++){if(cursor+46>bytes.length||view.getUint32(cursor,true)!==0x02014b50)throw new Error('ZIP中央目录条目损坏。');const flags=view.getUint16(cursor+8,true),method=view.getUint16(cursor+10,true),expectedCRC=view.getUint32(cursor+16,true),compressed=view.getUint32(cursor+20,true),expanded=view.getUint32(cursor+24,true),nameLength=view.getUint16(cursor+28,true),extraLength=view.getUint16(cursor+30,true),commentLength=view.getUint16(cursor+32,true),localOffset=view.getUint32(cursor+42,true);if(flags&1)throw new Error('不接受加密工作包。');if(method!==0||compressed!==expanded)throw new Error('只接受本系统生成的无压缩安全工作包。');if(expanded>FILE_LIMIT)throw new Error('工作包内单个文件超过128MB。');total+=expanded;if(total>PACKAGE_LIMIT)throw new Error('单个工作包分卷展开后超过256MB。');const name=decoder.decode(bytes.slice(cursor+46,cursor+46+nameLength));if(!validPackagePath(name)||result.has(name))throw new Error(`工作包路径非法或重复：${name}`);if(view.getUint32(localOffset,true)!==0x04034b50)throw new Error('ZIP本地条目损坏。');const localNameLength=view.getUint16(localOffset+26,true),localExtraLength=view.getUint16(localOffset+28,true),start=localOffset+30+localNameLength+localExtraLength,end=start+expanded;if(end>bytes.length)throw new Error('ZIP文件内容越界。');const data=bytes.slice(start,end);if(crc32(data)!==expectedCRC)throw new Error(`CRC校验失败：${name}`);result.set(name,data);cursor+=46+nameLength+extraLength+commentLength}return result}
function parseDocument(entries,path){const data=entries.get(path);if(!data)throw new Error(`工作包缺少：${path}`);try{return JSON.parse(decoder.decode(data))}catch{throw new Error(`JSON损坏：${path}`)}}
async function objectEntries(model){const entries=[];for(const object of model.objects){const path=`objects/${object.object_type}/${object.object_id}.json`,data=encoder.encode(JSON.stringify(object));if(data.length>FILE_LIMIT)throw new Error(`单个对象超过128MB：${object.object_type} · ${object.object_id}`);entries.push({path,data,size:data.length,sha256:await sha256Bytes(data)})}for(const entry of model.binary_entries||[]){if(entry.size>FILE_LIMIT)throw new Error(`单个 TB 二进制超过128MB：${entry.path}`);entries.push(entry)}return entries.sort((a,b)=>a.path.localeCompare(b.path))}
async function v7ContentDigest(model,files){return sha256JSON({schema_version:SCHEMA_VERSION,export_mode:model.export_mode,workspace_id:model.workspace_id,package_id:model.package_id,snapshot_id:model.snapshot_id,base_snapshot_id:model.base_snapshot_id,base_content_digest:model.base_content_digest,counts:model.counts||{},files})}
async function packageParts(model){const entries=await objectEntries(model),descriptors=entries.map(({path,size,sha256})=>({path,size,sha256}));if(entries.length>TOTAL_ENTRY_LIMIT)throw new Error(`工作包文件总数超过 ${TOTAL_ENTRY_LIMIT}。`);if(entries.reduce((sum,item)=>sum+item.size,0)>TOTAL_PACKAGE_LIMIT)throw new Error('工作包合计超过 2GB 安全上限。');const contentDigest=await v7ContentDigest(model,descriptors),groups=[];model.content_digest=contentDigest;let current=[],size=0;for(const entry of entries){if(current.length&&(size+entry.size>RECOMMENDED_PART_LIMIT||current.length>=ENTRY_LIMIT-1)){groups.push(current);current=[];size=0}current.push(entry);size+=entry.size}if(current.length)groups.push(current);if(!groups.length)throw new Error('工作包没有可导出的对象。');if(groups.length>PART_LIMIT)throw new Error(`工作包超过 ${PART_LIMIT} 卷安全上限。`);return Promise.all(groups.map(async(group,index)=>{const files=group.map(({path,size,sha256})=>({path,size,sha256})),partDigest=await sha256JSON(files),manifest={schema_version:SCHEMA_VERSION,export_mode:model.export_mode,workspace_id:model.workspace_id,package_id:model.package_id,snapshot_id:model.snapshot_id,base_snapshot_id:model.base_snapshot_id,base_content_digest:model.base_content_digest,generated_at:model.generated_at,part_index:index+1,part_count:groups.length,content_digest:contentDigest,part_digest:partDigest,counts:model.counts,warnings:model.warnings,limits:{recommended_part_bytes:RECOMMENDED_PART_LIMIT,max_part_bytes:PACKAGE_LIMIT,max_file_bytes:FILE_LIMIT,max_entries:ENTRY_LIMIT,max_total_bytes:TOTAL_PACKAGE_LIMIT,max_parts:PART_LIMIT,max_total_entries:TOTAL_ENTRY_LIMIT,max_media_blob_bytes:MEDIA_BLOB_LIMIT},files},manifestData=encoder.encode(JSON.stringify(manifest,null,2));return{manifest,files:[{name:'manifest.json',data:manifestData},...group.map(entry=>({name:entry.path,data:entry.data}))]}}))}
function downloadBlob(blob,name){const url=URL.createObjectURL(blob),anchor=document.createElement('a');anchor.href=url;anchor.download=name;document.body.appendChild(anchor);anchor.click();anchor.remove();setTimeout(()=>URL.revokeObjectURL(url),2000)}
async function exportPackage(exportMode='full'){setWorkspaceStatus(exportMode==='incremental'?'正在生成自上次成功打包后的增量…':'正在生成完整工作包…');const model=await buildWorkspaceModel(exportMode);if(exportMode==='incremental'&&!model.objects.length)throw new Error('自上次成功打包后没有新增或修改。');const parts=await packageParts(model),stamp=compactLocalTimestamp(),label=exportMode==='incremental'?'增量':'全量';for(const part of parts){const blob=zipStore(part.files);if(blob.size>PACKAGE_LIMIT)throw new Error('分卷超过256MB，导出已停止。');downloadBlob(blob,`Ghgy-客户${label}工作包-${stamp}-part${String(part.manifest.part_index).padStart(2,'0')}-of-${String(part.manifest.part_count).padStart(2,'0')}.zip`)}model.identity_state.last_snapshot_id=model.snapshot_id;model.identity_state.last_snapshot_digest=model.content_digest;model.identity_state.last_snapshot_objects=Object.fromEntries(model.all_objects.map(item=>[item.object_id,item]));await setMeta('object_identity_state',model.identity_state);await setMeta('last_backup_at',nowISO());setWorkspaceStatus(`${label}工作包已导出：${model.counts.objects}个新增/修改对象，共${parts.length}卷。回传时请一次性选择全部分卷。`);updateStorageUI()}

async function validateDeclared(entries,manifest,usePartDigest=false){if(!Array.isArray(manifest.files)||manifest.files.length>ENTRY_LIMIT-1)throw new Error('manifest文件清单无效。');const declared=new Set(['manifest.json']);for(const item of manifest.files){assertKeys(item,['path','size','sha256'],'manifest.files');if(!validPackagePath(item.path)||declared.has(item.path))throw new Error(`manifest路径非法或重复：${item.path}`);const data=entries.get(item.path);if(!data||data.length!==item.size)throw new Error(`文件缺失或大小不符：${item.path}`);if(await sha256Bytes(data)!==item.sha256)throw new Error(`SHA-256校验失败：${item.path}`);declared.add(item.path)}if([...entries.keys()].some(path=>!declared.has(path)))throw new Error('工作包包含manifest未声明的文件。');const digest=await sha256JSON(manifest.files.map(({path,size,sha256})=>({path,size,sha256})));if(digest!==(usePartDigest?manifest.part_digest:manifest.content_digest))throw new Error('工作包内容摘要不一致。')}
function bytesDataURL(bytes){let binary='';for(let offset=0;offset<bytes.length;offset+=32768)binary+=String.fromCharCode(...bytes.subarray(offset,Math.min(bytes.length,offset+32768)));return`data:image/png;base64,${btoa(binary)}`}
async function hydrateV7MediaPayload(payload,packageFiles){const versions=payload?.versions;if(!Array.isArray(versions)||!versions.length||versions.length>100)throw new Error('V7 TB 必须包含 1 至 100 个不可变版本。');const versionRefs=new Set(),pageRefs=new Set(),hydrated=[];let pageTotal=0;for(const[versionIndex,version]of versions.entries()){if(!/^WV-[A-Z0-9]{16,64}$/.test(version.version_ref||'')||versionRefs.has(version.version_ref)||Number(version.version_no)!==versionIndex+1)throw new Error('V7 TB 版本身份无效、重复或不连续。');versionRefs.add(version.version_ref);if(!Array.isArray(version.pages)||!version.pages.length||version.pages.length>200)throw new Error('V7 TB 每个版本必须包含 1 至 200 页。');const pages=[];for(const[pageIndex,page]of version.pages.entries()){const bytes=packageFiles.get(page.blob_path);if(!/^WP-[A-Z0-9]{16,64}$/.test(page.page_ref||'')||pageRefs.has(page.page_ref)||Number(page.page_no)!==pageIndex+1)throw new Error('V7 TB 页面身份无效、重复或不连续。');if(!/^blobs\/sha256\/[a-f0-9]{2}\/[a-f0-9]{64}\.png$/.test(page.blob_path||'')||!bytes||bytes.length!==Number(page.byte_size)||bytes.length>MEDIA_BLOB_LIMIT||page.mime_type!=='image/png'||await sha256Bytes(bytes)!==page.blob_sha256||!String(page.blob_path).endsWith(`/${page.blob_sha256}.png`))throw new Error('V7 TB 二进制缺失、超限或摘要损坏。');if(bytes.length<24||bytes[0]!==137||bytes[1]!==80||bytes[2]!==78||bytes[3]!==71||bytes[12]!==73||bytes[13]!==72||bytes[14]!==68||bytes[15]!==82)throw new Error('V7 TB PNG 文件头损坏。');const view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);if(view.getUint32(16)!==1550||view.getUint32(20)!==700||Number(page.width)!==1550||Number(page.height)!==700)throw new Error('V7 TB 页面必须是 1550×700 PNG。');const hydratedPage={...page,preview_png:bytesDataURL(bytes)};if(page.hd_blob_path){const hdBytes=packageFiles.get(page.hd_blob_path);if(!/^blobs\/sha256\/[a-f0-9]{2}\/[a-f0-9]{64}\.png$/.test(page.hd_blob_path)||!hdBytes||hdBytes.length!==Number(page.hd_byte_size)||hdBytes.length>MEDIA_BLOB_LIMIT||page.hd_mime_type!=='image/png'||await sha256Bytes(hdBytes)!==page.hd_blob_sha256||!String(page.hd_blob_path).endsWith(`/${page.hd_blob_sha256}.png`))throw new Error('V7 TB 高清二进制缺失、超限或摘要损坏。');if(hdBytes.length<24)throw new Error('V7 TB 高清 PNG 文件头损坏。');const hdView=new DataView(hdBytes.buffer,hdBytes.byteOffset,hdBytes.byteLength);if(hdView.getUint32(16)!==3100||hdView.getUint32(20)!==1400||Number(page.hd_width)!==3100||Number(page.hd_height)!==1400)throw new Error('V7 TB 高清页面必须是 3100×1400 PNG。');hydratedPage.hd_preview_png=bytesDataURL(hdBytes)}pageRefs.add(page.page_ref);pageTotal++;pages.push(hydratedPage)}hydrated.push({...version,pages})}if(pageTotal>10000)throw new Error('V7 TB 页面总数超过 10000。');if(payload.current_version_ref!==hydrated.at(-1).version_ref)throw new Error('V7 TB 当前版本必须指向最后一个不可变版本。');const current=hydrated.at(-1);return{...payload,versions:hydrated,pages:clone(current.pages),config:clone(current.config||{}),preview_png:current.pages[0].preview_png,hd_preview_png:current.pages[0].hd_preview_png||null}}
async function validatePackage(files){
  const selected=[...files];if(!selected.length)throw new Error('请选择工作包。');if(selected.length>PART_LIMIT)throw new Error(`工作包分卷超过 ${PART_LIMIT} 卷安全上限。`);if(selected.reduce((sum,file)=>sum+Number(file.size||0),0)>TOTAL_PACKAGE_LIMIT)throw new Error('工作包分卷合计超过 2GB 安全上限。');setWorkspaceStatus('正在本机解析并校验工作包，不会写入草稿…');
  const parsed=[];for(const file of selected){const entries=await parseZip(file),manifest=parseDocument(entries,'manifest.json');if(!SUPPORTED_SCHEMA_VERSIONS.has(manifest.schema_version))throw new Error(`不兼容的工作包版本：${manifest.schema_version||'未知'}`);await validateDeclared(entries,manifest,OBJECT_SCHEMAS.has(manifest.schema_version));parsed.push({entries,manifest})}
  const schema=parsed[0].manifest.schema_version;if(parsed.some(item=>item.manifest.schema_version!==schema))throw new Error('所选分卷协议版本不一致。');if(!OBJECT_SCHEMAS.has(schema)){if(parsed.length!==1)throw new Error('v1/v2/v3老工作包不支持多分卷。');const{entries,manifest}=parsed[0],derived=parseDocument(entries,'drafts/indicators.json'),charts=parseDocument(entries,'drafts/charts.json'),showcase=parseDocument(entries,'workspace/showcase.json'),domains=parseDocument(entries,'workspace/domains.json'),meta=parseDocument(entries,'workspace/meta.json'),displayNames=schema==='ghgy-workspace-v1'?[]:parseDocument(entries,'workspace/display-names.json');const payload={manifest,derived,charts,showcase,domains,meta,displayNames};assertSafePackage(payload);return payload}
  const first=parsed[0].manifest,identity=[first.workspace_id,first.package_id,first.snapshot_id].join('|'),coreFields=['export_mode','base_snapshot_id','base_content_digest','generated_at','part_count','content_digest','counts','warnings'];if(parsed.some(item=>[item.manifest.workspace_id,item.manifest.package_id,item.manifest.snapshot_id].join('|')!==identity))throw new Error('所选分卷不属于同一工作包快照。');if(parsed.some(item=>coreFields.some(field=>stableJSON(item.manifest[field])!==stableJSON(first[field]))))throw new Error('工作包分卷核心清单不一致。');if(Number(first.part_count)!==parsed.length||parsed.map(item=>Number(item.manifest.part_index)).sort((a,b)=>a-b).some((value,index)=>value!==index+1))throw new Error('工作包分卷不完整或编号重复。');
  const descriptors=parsed.sort((a,b)=>a.manifest.part_index-b.manifest.part_index).flatMap(item=>item.manifest.files).map(({path,size,sha256})=>({path,size,sha256}));if(descriptors.length>TOTAL_ENTRY_LIMIT||new Set(descriptors.map(item=>item.path)).size!==descriptors.length)throw new Error('工作包文件总数超限或跨分卷路径重复。');const digest=schema===SCHEMA_VERSION?await v7ContentDigest(first,descriptors):await sha256JSON(descriptors);if(parsed.some(item=>item.manifest.content_digest!==digest))throw new Error('工作包完整内容摘要不一致。');
  const packageFiles=new Map();for(const{entries}of parsed)for(const[path,data]of entries)if(path!=='manifest.json')packageFiles.set(path,data);const objects=[];for(const{entries,manifest}of parsed)for(const item of manifest.files){if(!item.path.startsWith('objects/'))continue;const value=parseDocument(entries,item.path);assertKeys(value,['object_id','object_type','revision','content_sha256','lifecycle_status','dependencies','payload'],item.path);if(value.content_sha256!==await sha256JSON(value.payload))throw new Error(`对象内容摘要不一致：${item.path}`);objects.push(value)}if(new Set(objects.map(item=>item.object_id)).size!==objects.length)throw new Error('工作包包含重复object_id。');if(Number(first.counts?.objects)!==objects.length)throw new Error('对象数量与manifest不一致。');assertSafePackage(objects);if(schema===SCHEMA_VERSION)for(const object of objects)if(object.object_type==='media')object.payload=await hydrateV7MediaPayload(object.payload,packageFiles);return{manifest:first,v4Objects:objects}
}
function assertSafePackage(value){const text=stableJSON(value);if(TECHNICAL_ID.test(text)){TECHNICAL_ID.lastIndex=0;throw new Error('工作包包含伪造内部身份或公开运行关联键。')}TECHNICAL_ID.lastIndex=0;if(/<\s*script\b|javascript\s*:|onerror\s*=|onload\s*=/i.test(text))throw new Error('工作包包含脚本载荷。')}
function findPublicIndicator(descriptor){if(descriptor?.feedback_ref){const match=publicIndicators().find(item=>item.feedback_ref===descriptor.feedback_ref);if(match)return match.indicator_id;throw new Error('正式指标稳定身份已不存在。')}const target=descriptorKey(descriptor),matches=publicIndicators().filter(item=>descriptorKey(safeDescriptor(item))===target);if(matches.length!==1)throw new Error(`老工作包正式依赖无法唯一匹配：${descriptor?.name||'未命名指标'}`);return matches[0].indicator_id}
function findPublicChart(value){const feedbackRef=value?.feedback_ref||value;const stable=publicCharts().find(chart=>chart.feedback_ref===feedbackRef);if(stable)return stable.chart_id;if(typeof value==='object'){const matches=publicCharts().filter(chart=>cleanText(chart.title)===value.title&&cleanText(chart.domain)===value.domain);return matches.length===1?matches[0].chart_id:null}return null}
function restoreOrganizationClassifications(value){const custom=classificationTermStore(),dimensions=Object.fromEntries((window.GALLERY_BOOTSTRAP?.workbench_contract?.classification_dimensions||[]).map(item=>[item.dimension_key,{...item,terms:[...(item.terms||[]),...(Array.isArray(custom[item.dimension_key])?custom[item.dimension_key]:[])]}]));return Object.fromEntries(Object.entries(value||{}).filter(([key])=>dimensions[key]).map(([key,names])=>{const terms=dimensions[key]?.terms||[];return[key,(names||[]).map((name,index)=>{const clean=cleanClassificationName(name);if(clean.length>80)throw new Error('工作包中的分类标签超过80个字符。');const match=terms.find(term=>classificationNameKey(term.term_name)===classificationNameKey(clean));return match?{term_id:match.term_id,term_name:match.term_name,sort_order:match.sort_order??index}:{term_id:clientClassificationTermId(key,clean),term_name:clean,sort_order:10000+index,customer_local:true}}).filter(term=>term.term_name)]}))}
function restoreConfig(config,indicatorIdByObject){const restored=clone(config||{}),seriesKeyMap=new Map();restored.series=(config?.series||[]).map((row,index)=>{const instanceId=localId('SI'),copy=clone(row),dependency=row.dependency||{};seriesKeyMap.set(row.series_key,instanceId);delete copy.series_key;copy.instance_id=instanceId;copy.indicator_id=dependency.object_ref?indicatorIdByObject.get(dependency.object_ref):dependency.draft_key?indicatorIdByObject.get(dependency.draft_key):findPublicIndicator(dependency);delete copy.dependency;return copy});if(restored.time_alignment){restored.time_alignment.reference_instance_id=seriesKeyMap.get(restored.time_alignment.reference_series_key)||'';delete restored.time_alignment.reference_series_key}return restored}
function remapImportTree(value,chartIdByRef,folderIdByRef,indicatorIdByRef=restoreIndicatorRefs){if(Array.isArray(value))return value.map(item=>remapImportTree(item,chartIdByRef,folderIdByRef,indicatorIdByRef));if(value&&typeof value==='object'){const result={};for(const[key,item]of Object.entries(value)){if(key==='chart_ref'){result.chart_id=chartIdByRef.get(item)||findPublicChart(item);continue}if(key==='folder_ref'){result.folder_id=folderIdByRef.get(item)||null;continue}if(key==='indicator_ref'){result.indicator_id=indicatorIdByRef.get(item)||findPublicIndicator({feedback_ref:item});continue}const mappedKey=chartIdByRef.get(key)||folderIdByRef.get(key)||key;result[mappedKey]=remapImportTree(item,chartIdByRef,folderIdByRef,indicatorIdByRef)}return result}if(typeof value==='string')return chartIdByRef.get(value)||folderIdByRef.get(value)||findPublicChart(value)||value;return value}
async function materializeV4(payload){const identity=await identityContext(),incremental=payload.manifest.schema_version===SCHEMA_VERSION&&payload.manifest.export_mode==='incremental';if(incremental&&(identity.saved.last_snapshot_id!==payload.manifest.base_snapshot_id||identity.saved.last_snapshot_digest!==payload.manifest.base_content_digest))throw new Error('本机缺少该增量包基线，请改用“导出全部工作（迁移/恢复）”。');const byType=type=>payload.v4Objects.filter(item=>item.object_type===type),reverse=new Map(Object.entries(identity.saved.objects).map(([key,value])=>[value.object_id,key])),localKey=(object,prefix)=>{const known=reverse.get(object.object_id)?.split(':').slice(1).join(':');return known||localId(prefix)},indicatorIdByObject=new Map(byType('indicator').map(object=>[object.object_id,localKey(object,'LOCAL-DERIVED')])),chartIdByRef=new Map(byType('chart').map(object=>[object.object_id,localKey(object,'LOCAL-CH')])),folderIdByRef=new Map(byType('folder').map(object=>[object.object_id,localKey(object,'BLF')])),compositionIdByRef=new Map(byType('composition').map(object=>[object.object_id,localKey(object,'BLC')]));restoreIndicatorRefs=new Map(publicIndicators().filter(item=>item.feedback_ref).map(item=>[item.feedback_ref,item.indicator_id]));for(const[objectRef,indicatorId]of indicatorIdByObject)restoreIndicatorRefs.set(objectRef,indicatorId);for(const chart of publicCharts())if(chart.feedback_ref)chartIdByRef.set(chart.feedback_ref,chart.chart_id);const derived=byType('indicator').map(object=>{const id=indicatorIdByObject.get(object.object_id),payload=object.payload,definition=clone(payload.definition),inputs=(definition.config?.inputs||[]).map(input=>{const copy=clone(input),dependency=input.dependency||{};copy.indicator_id=dependency.object_ref?indicatorIdByObject.get(dependency.object_ref):findPublicIndicator(dependency);delete copy.dependency;return copy});definition.config={...(definition.config||{}),inputs,derived_id:id};definition.derived_id=id;definition.indicator_id=id;const observations=(payload.observations||[]).map(point=>({date:point.date,value:Number(point.value),value_display:String(point.value)})),metadata={...(payload.metadata||{}),indicator_id:id,external_id:id,source_system:'浏览器草稿',publisher:'客户本机草稿',access_level:'browser_local',first_date:observations[0]?.date||null,latest_date:observations.at(-1)?.date||null,latest_value:observations.at(-1)?.value??null};return{indicator:metadata,definition,series:observations}}),charts=byType('chart').map(object=>{const id=chartIdByRef.get(object.object_id),item=object.payload,config=restoreConfig(item.config,indicatorIdByObject);return{chart_id:id,title:item.title||'未命名草稿',domain:item.domain||'未分类',status:'browser_draft',access_level:'browser_local',creation_source:'browser_local',lifecycle_status:object.lifecycle_status,config:{...config,chart_id:id,title:item.title||'未命名草稿',status:'browser_draft',access_level:'browser_local'},html_url:null,png_url:item.preview_png||null,interactive_figure:item.figure||null,figure:null,updated_at:item.updated_at||nowISO(),render_fingerprint:item.render?.fingerprint||null,data_snapshot_hash:item.render?.data_snapshot_hash||null,renderer_version:item.render?.renderer_version||null,note_text:item.note?.text||'',note_version:Number(item.note?.version||0)}}),organizationOverrides={};for(const object of byType('chart_organization')){const organization={is_pinned:Boolean(object.payload.is_pinned),pin_rank:object.payload.pin_rank,metadata_revision:object.revision,organization_updated_at:nowISO(),classifications:restoreOrganizationClassifications(object.payload.classifications)},chart=charts.find(item=>item.chart_id===chartIdByRef.get(object.payload.chart_ref));if(chart)chart.organization=organization;else if(/^FCR-[A-F0-9]{20}$/.test(object.payload.chart_ref)&&publicCharts().some(item=>item.feedback_ref===object.payload.chart_ref))organizationOverrides[object.payload.chart_ref]=organization}const folders=byType('folder').map(object=>({...clone(object.payload),folder_id:folderIdByRef.get(object.object_id),access_level:'browser_local'})),compositions=byType('composition').map(object=>({...remapImportTree(object.payload,chartIdByRef,folderIdByRef),composition_id:compositionIdByRef.get(object.object_id),access_level:'private'})),layoutObject=byType('showcase_layout')[0],showcase=layoutObject?remapImportTree(layoutObject.payload,chartIdByRef,folderIdByRef):{selected:[],order:[],layout:{}},preferences=byType('workspace_preferences')[0]?.payload||{},displayNames={indicator:{},chart:{}};for(const object of byType('rename')){const item=object.payload,rows=item.entity_type==='indicator'?publicIndicators():publicCharts(),match=rows.find(row=>row.feedback_ref===item.feedback_ref);if(match){const id=item.entity_type==='indicator'?match.indicator_id:match.chart_id;displayNames[item.entity_type][id]={display_name:item.customer_name,version:1,updated_at:item.updated_at||nowISO(),history:[]}}}const notes=Object.fromEntries(charts.filter(chart=>chart.note_text).map(chart=>[chart.chart_id,{note_text:chart.note_text,version:chart.note_version||1}]));for(const object of payload.v4Objects){const key=object.object_type==='indicator'?indicatorIdByObject.get(object.object_id):object.object_type==='chart'?chartIdByRef.get(object.object_id):object.object_type==='folder'?folderIdByRef.get(object.object_id):object.object_type==='composition'?compositionIdByRef.get(object.object_id):object.object_type==='chart_organization'?(String(object.payload.chart_ref||'').startsWith('FCR-')?`public:${object.payload.chart_ref}`:chartIdByRef.get(object.payload.chart_ref)):null;if(key)identity.saved.objects[`${object.object_type}:${key}`]={object_id:object.object_id,revision:object.revision,content_sha256:object.content_sha256}}identity.saved.workspace_id=payload.manifest.workspace_id;identity.saved.last_snapshot_id=payload.manifest.snapshot_id;identity.saved.last_snapshot_digest=payload.manifest.content_digest;const snapshotObjects=incremental?{...(identity.saved.last_snapshot_objects||{})}:{};for(const object of payload.v4Objects){if(object.lifecycle_status==="delete_requested")delete snapshotObjects[object.object_id];else snapshotObjects[object.object_id]=object}identity.saved.last_snapshot_objects=snapshotObjects;return{derived,charts,notes,displayNames,organizationOverrides,domains:preferences.custom_domains||[],showcase:showcase.selected||[],order:showcase.order||[],layout:showcase.layout||{},compositions,folders,identityState:identity.saved}}
const materializeV4BeforeMedia=materializeV4;
function restoreMediaPageReferences(value,mediaIdByRef,portableByRef=new Map()){const restore=value=>{const match=String(value||'').match(/^media-page:(WO-[A-Z0-9]{16,64}):(\d+)$/),localIdValue=match&&mediaIdByRef.get(match[1]);return localIdValue?`tb-local:${localIdValue}:${Number(match[2])}`:value};if(Array.isArray(value))return value.map(item=>restoreMediaPageReferences(item,mediaIdByRef,portableByRef));if(value&&typeof value==='object'){const result={};for(const[key,item]of Object.entries(value)){if(key==='media_refs')continue;if(key==='media_ref'){result.local_media_id=mediaIdByRef.get(item)||null;continue}if(key==='media_version_ref'){const mapped=portableByRef.get(item);if(mapped){result.local_version_id=mapped.local_version_id;result.browser_version_key=mapped.browser_version_key}continue}if(key==='media_page_ref'){const mapped=portableByRef.get(item);if(mapped)result.local_page_id=mapped.local_page_id;continue}result[restore(key)]=restoreMediaPageReferences(item,mediaIdByRef,portableByRef)}return result}return typeof value==='string'?restore(value):value}
materializeV4=async function(payload){
  const normalized=['ghgy-workspace-v5','ghgy-workspace-v6'].includes(payload.manifest?.schema_version)&&payload.manifest?.export_mode==='incremental'?{...payload,manifest:{...payload.manifest,schema_version:SCHEMA_VERSION}}:payload,result=await materializeV4BeforeMedia(normalized),reverse=new Map(Object.entries(result.identityState?.objects||{}).map(([key,value])=>[value.object_id,key])),media=[],mediaIdByRef=new Map(),portableByRef=new Map();result.identityState.media_versions=result.identityState.media_versions||{};result.identityState.media_pages=result.identityState.media_pages||{};
  for(const object of(payload.v4Objects||[]).filter(item=>item.object_type==='media')){
    const known=reverse.get(object.object_id)?.split(':').slice(1).join(':'),id=known||localId('LOCAL-MEDIA'),item=object.payload||{},assetKind=(item.asset_kind||item.media_kind)==='table'?'table':'screenshot',sourceVersions=Array.isArray(item.versions)&&item.versions.length?item.versions:[{version_no:1,title:item.title,config:item.config||{},pages:item.pages||[{preview_png:item.preview_png,config:{}}],created_at:item.created_at}],versions=[];mediaIdByRef.set(object.object_id,id);
    for(const[sourceIndex,sourceVersion]of sourceVersions.entries()){const localVersionId=localId('LOCAL-MEDIA-V'),browserVersionKey=localId('LOCAL-MEDIA-VK'),pages=(sourceVersion.pages||[]).map((page,index)=>{const localPageId=localId('LOCAL-MEDIA-P');if(page.page_ref){portableByRef.set(page.page_ref,{local_page_id:localPageId});result.identityState.media_pages[localPageId]=page.page_ref}return{preview_png:page.preview_png,config:clone(page.config||{}),local_page_id:localPageId,page_no:index+1}}),version={local_version_id:localVersionId,browser_version_key:browserVersionKey,version_no:sourceIndex+1,title:sourceVersion.title||item.title,config:clone(sourceVersion.config||item.config||{}),pages,created_at:sourceVersion.created_at||item.created_at||nowISO()};if(sourceVersion.version_ref){portableByRef.set(sourceVersion.version_ref,{local_version_id:localVersionId,browser_version_key:browserVersionKey});result.identityState.media_versions[localVersionId]=sourceVersion.version_ref}versions.push(version)}
    const current=versions.at(-1),record={media_id:id,title:item.title||'未命名素材',asset_kind:assetKind,media_kind:assetKind==='screenshot'?'image':'table',status:'browser_draft',publication_status:'draft',access_level:'browser_local',requested_access_level:item.requested_access_level||'private',config:clone(current.config||{}),versions,current_version_id:current.local_version_id,current_version_no:current.version_no,browser_version_key:current.browser_version_key,pages:clone(current.pages),page_count:current.pages.length,preview_png:current.pages[0]?.preview_png||null,png_url:current.pages[0]?.preview_png||null,width:1550,height:700,created_at:item.created_at||nowISO(),updated_at:item.updated_at||nowISO(),lifecycle_status:object.lifecycle_status||'active'};media.push(record);result.identityState.objects[`media:${id}`]={object_id:object.object_id,revision:object.revision,content_sha256:object.content_sha256}
  }
  for(const object of(payload.v4Objects||[]).filter(item=>item.object_type==='media_organization')){const id=mediaIdByRef.get(object.payload?.media_ref),record=media.find(item=>item.media_id===id);if(!record)continue;record.is_pinned=Boolean(object.payload.is_pinned);record.pin_rank=object.payload.pin_rank??null;record.classifications=restoreOrganizationClassifications(object.payload.classifications||{});result.identityState.objects[`media_organization:${id}`]={object_id:object.object_id,revision:object.revision,content_sha256:object.content_sha256}}
  result.media=media;result.compositions=restoreMediaPageReferences(result.compositions||[],mediaIdByRef,portableByRef);result.showcase=restoreMediaPageReferences(result.showcase||[],mediaIdByRef,portableByRef);result.order=restoreMediaPageReferences(result.order||[],mediaIdByRef,portableByRef);result.layout=restoreMediaPageReferences(result.layout||{},mediaIdByRef,portableByRef);return result
};
function materializeLegacy(payload){const draftIdByKey=new Map(payload.derived.map(item=>[item.draft_key,localId('LOCAL-DERIVED')])),chartIdByRef=new Map(),derived=payload.derived.map(item=>{const id=draftIdByKey.get(item.draft_key),definition=clone(item.definition),inputs=(definition.config?.inputs||[]).map(input=>{const copy=clone(input);copy.indicator_id=input.dependency?.kind==='draft_dependency'?draftIdByKey.get(input.dependency.draft_key):findPublicIndicator(input.dependency||{});delete copy.dependency;return copy});definition.config={...(definition.config||{}),inputs,derived_id:id};definition.derived_id=id;definition.indicator_id=id;const observations=(item.observations||[]).map(point=>({date:point.date,value:Number(point.value),value_display:String(point.value)})),metadata={...(item.metadata||{}),indicator_id:id,external_id:id,source_system:'浏览器草稿',publisher:'客户本机草稿',access_level:'browser_local',first_date:observations[0]?.date||null,latest_date:observations.at(-1)?.date||null,latest_value:observations.at(-1)?.value??null};return{indicator:metadata,definition,series:observations}}),charts=payload.charts.map(item=>{const id=localId('LOCAL-CH');chartIdByRef.set(item.draft_key,id);const config=restoreConfig(item.config,draftIdByKey);return{chart_id:id,title:item.title||'未命名草稿',domain:item.domain||'未分类',status:'browser_draft',access_level:'browser_local',creation_source:'browser_local',config:{...config,chart_id:id,title:item.title||'未命名草稿',status:'browser_draft',access_level:'browser_local'},html_url:null,png_url:item.preview_png||null,interactive_figure:item.figure||null,figure:null,updated_at:nowISO(),data_snapshot_hash:item.render?.data_snapshot_hash||null,renderer_version:item.render?.renderer_version||null,note_text:item.note?.text||'',note_version:Number(item.note?.version||0)}});for(const item of payload.showcase.public_dependencies||[]){const id=findPublicChart(item.descriptor||{});if(id)chartIdByRef.set(item.ref,id)}const remap=value=>{if(Array.isArray(value))return value.map(remap);if(value&&typeof value==='object'){const result={};for(const[key,item]of Object.entries(value))result[chartIdByRef.get(key)||key]=remap(item);return result}return typeof value==='string'&&chartIdByRef.has(value)?chartIdByRef.get(value):value},displayNames={indicator:{},chart:{}};for(const item of payload.displayNames||[]){const rows=item.entity_type==='indicator'?publicIndicators():publicCharts(),match=rows.filter(row=>item.entity_type==='indicator'?descriptorKey(safeDescriptor(row))===descriptorKey(item.descriptor):cleanText(row.title)===cleanText(item.descriptor?.title));if(match.length===1){const id=item.entity_type==='indicator'?match[0].indicator_id:match[0].chart_id;displayNames[item.entity_type][id]={display_name:cleanText(item.display_name),version:1,updated_at:item.updated_at||nowISO(),history:[]}}}const notes=Object.fromEntries(charts.filter(chart=>chart.note_text).map(chart=>[chart.chart_id,{note_text:chart.note_text,version:chart.note_version||1}]));return{derived,charts,media:[],notes,displayNames,organizationOverrides:{},domains:payload.domains,showcase:remap(payload.showcase.selected||[]).filter(Boolean),order:remap(payload.showcase.order||[]).filter(Boolean),layout:remap(payload.showcase.layout||{}),compositions:remap(payload.showcase.compositions||[]),folders:[]}}
function normalizeImportedChartActivity(result){for(const chart of result?.charts||[]){const activity=chart.manual_activity_at||chart.updated_at||nowISO();chart.created_at=chart.created_at||activity;chart.manual_activity_at=activity;chart.updated_at=chart.updated_at||activity}return result}
const materializeV4BeforeChartActivity=materializeV4;
materializeV4=async payload=>normalizeImportedChartActivity(await materializeV4BeforeChartActivity(payload));
const materializeLegacyBeforeChartActivity=materializeLegacy;
materializeLegacy=payload=>normalizeImportedChartActivity(materializeLegacyBeforeChartActivity(payload));
function mergeBy(rows,incoming,key){const value=item=>key.split('.').reduce((current,part)=>current?.[part],item),map=new Map(rows.map(item=>[value(item),item]));for(const item of incoming)map.set(value(item),item);return[...map.values()]}
async function workspaceFingerprint(){const stores={};for(const[collection,descriptor]of Object.entries(HEAVY_COLLECTIONS))stores[collection]=(await idbGetAll(descriptor.store)).map(item=>[item.id,item.content_sha256]).sort();const durable={};for(const key of DURABLE_VALUE_KEYS){const record=await idbGetRecord(RECORD_STORE,key);durable[key]=record?.content_sha256||null}return sha256JSON({stores,durable,identity:await getMeta('object_identity_state',null)})}
async function commitWorkspaceStateAtomic(values,identityState,restoreMeta){
  if(pendingImport?.workspace_fingerprint&&await workspaceFingerprint()!==pendingImport.workspace_fingerprint)throw new Error('确认期间本机草稿发生变化；未执行恢复，请重新预览。');
  if(restoreMeta.mode==='merge'){const current=await getMeta('object_identity_state',{}),sameWorkspace=!current?.workspace_id||current.workspace_id===identityState?.workspace_id;identityState={...current,...(sameWorkspace?identityState:{}),workspace_id:current?.workspace_id||identityState?.workspace_id,objects:{...(current?.objects||{}),...(identityState?.objects||{})},media_versions:{...(current?.media_versions||{}),...(identityState?.media_versions||{})},media_pages:{...(current?.media_pages||{}),...(identityState?.media_pages||{})},last_snapshot_id:sameWorkspace?identityState?.last_snapshot_id:null,last_snapshot_digest:sameWorkspace?identityState?.last_snapshot_digest:null,last_snapshot_objects:sameWorkspace?identityState?.last_snapshot_objects:{}}}
  const prepared={},existingSize=(await Promise.all(Object.values(HEAVY_COLLECTIONS).map(descriptor=>idbGetAll(descriptor.store)))).flat().reduce((sum,row)=>sum+Number(row.byte_size||0),0);let nextSize=0;
  for(const[key,descriptor]of Object.entries(HEAVY_COLLECTIONS)){const previous=await idbGetAll(descriptor.store),records=[];for(const item of mergeCollectionRows(key,values[key]||[])){const id=descriptor.id(item),record=await prepareStoredRecord(key,item,previous.find(row=>row.id===id)||null);records.push(record);nextSize+=record.byte_size}prepared[key]=records}
  await ensureStorageHeadroom(Math.max(0,nextSize-existingSize));const durableRecords=[];for(const key of DURABLE_VALUE_KEYS){const text=JSON.stringify(values[key]??(key===CLASSIFICATION_TERMS_STORAGE_KEY?{}:[]));durableRecords.push({key,value:text,updated_at:nowISO(),content_sha256:await sha256JSON(text)})}
  const db=await openDB(),storeNames=[CHART_STORE,DERIVED_STORE,MEDIA_STORE,RECORD_STORE,META_STORE];await new Promise((resolve,reject)=>{const tx=db.transaction(storeNames,'readwrite');for(const[key,descriptor]of Object.entries(HEAVY_COLLECTIONS)){const store=tx.objectStore(descriptor.store);store.clear();for(const record of prepared[key])store.put(record)}const records=tx.objectStore(RECORD_STORE);for(const key of DURABLE_VALUE_KEYS)records.delete(key);for(const record of durableRecords)records.put(record);const meta=tx.objectStore(META_STORE);meta.put({key:'object_identity_state',value:JSON.stringify(identityState||{}),updated_at:nowISO()});meta.put({key:'last_restore_at',value:JSON.stringify(restoreMeta),updated_at:nowISO()});meta.put({key:'last_saved_at',value:JSON.stringify(restoreMeta.at),updated_at:nowISO()});tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error||new Error('恢复事务失败。'));tx.onabort=()=>reject(tx.error||new Error('恢复事务已中止；原草稿保持不变。'))});
  for(const[key,descriptor]of Object.entries(HEAVY_COLLECTIONS)){const saved=await idbGetAll(descriptor.store);if(saved.length!==prepared[key].length)throw new Error('恢复后的独立数量校验失败。');for(const record of saved)if(await recordDigest(record)!==record.content_sha256)throw new Error('恢复后的独立哈希校验失败。')}for(const record of durableRecords)if(!storageValueEqual(await validatedDurableValue(record.key),record.value))throw new Error('恢复后的本机设置校验失败。');clearLegacyDraftKeys();localStorage.setItem(WORKSPACE_MUTATION_SIGNAL_KEY,`${restoreMeta.at}:${crypto.getRandomValues(new Uint32Array(1))[0]}`)
}
async function applyImport(mode){
  assertWorkspaceMutable();if(!pendingImport)throw new Error('请先选择并预览工作包；未通过预览时不能替换恢复。');if(await workspaceFingerprint()!==pendingImport.workspace_fingerprint)throw new Error('预览后本机草稿已发生变化；请重新选择工作包并预览。');if(mode==='replace'){if(!confirm('将用刚才预览通过的工作包替换当前全部本机草稿。请先导出备份。是否继续？'))return;if(!confirm('第二次确认：清空当前本机草稿，并写入刚才预览的工作包？'))return}
  workspaceMutationBlocked=true;setWorkspaceActionDisabled(true);try{await waitForWorkspaceWrites();const incoming=pendingImport.v4Objects?await materializeV4(pendingImport):materializeLegacy(pendingImport),current={derived:parseArray('ghgy-local-derived-drafts-v1'),charts:parseArray('yd-local-charts'),media:parseArray('ghgy-local-media-drafts-v1'),notes:parseObject('yd-chart-notes'),displayNames:parseObject('ghgy-display-name-overrides-v1'),organizationOverrides:parseObject('ghgy-chart-organization-overrides-v1'),domains:parseArray('yd-custom-domains'),showcase:parseArray('yd-showcase'),order:parseArray('yd-showcase-layout-order-v1'),layout:parseObject('yd-showcase-layout-draft-v2'),compositions:parseArray('yd-showcase-compositions-v1'),folders:parseArray('yd-showcase-folders-v1')},next=mode==='replace'?incoming:{derived:mergeBy(current.derived,incoming.derived,'indicator.indicator_id'),charts:mergeBy(current.charts,incoming.charts,'chart_id'),media:mergeBy(current.media,incoming.media||[],'media_id'),notes:{...current.notes,...incoming.notes},displayNames:{indicator:{...(current.displayNames.indicator||{}),...(incoming.displayNames.indicator||{})},chart:{...(current.displayNames.chart||{}),...(incoming.displayNames.chart||{})}},organizationOverrides:{...current.organizationOverrides,...(incoming.organizationOverrides||{})},domains:[...new Set([...current.domains,...incoming.domains])],showcase:[...new Set([...current.showcase,...incoming.showcase])],order:[...current.order,...incoming.order],layout:{...current.layout,...incoming.layout},compositions:mergeBy(current.compositions,incoming.compositions,'composition_id'),folders:mergeBy(current.folders,incoming.folders,'folder_id')},importedTerms=importedClassificationTermStore(pendingImport),classificationTerms=mode==='replace'?importedTerms:mergeClassificationTermStores(classificationTermStore(),importedTerms),values={'ghgy-local-derived-drafts-v1':next.derived,'yd-local-charts':next.charts,'ghgy-local-media-drafts-v1':next.media||[],'yd-chart-notes':next.notes,'ghgy-display-name-overrides-v1':next.displayNames,'ghgy-chart-organization-overrides-v1':next.organizationOverrides,'yd-custom-domains':next.domains,'yd-showcase':next.showcase,'yd-showcase-layout-order-v1':next.order,'yd-showcase-layout-draft-v2':next.layout,'yd-showcase-compositions-v1':next.compositions,'yd-showcase-folders-v1':next.folders,[CLASSIFICATION_TERMS_STORAGE_KEY]:classificationTerms},restoreMeta={at:nowISO(),mode,schema_version:pendingImport.manifest.schema_version};await commitWorkspaceStateAtomic(values,incoming.identityState||await getMeta('object_identity_state',{}),restoreMeta);setWorkspaceStatus(`恢复完成：${incoming.derived.length}个指标、${incoming.charts.length}张图、${(incoming.media||[]).length}张表格/图片、${incoming.compositions.length}个组合、${incoming.folders.length}个文件夹。全部对象已在同一事务写入并复核，页面将刷新。`);setTimeout(()=>location.reload(),600)}catch(error){workspaceMutationBlocked=false;setWorkspaceActionDisabled(false);throw error}
}
function importedClassificationTermStore(payload){const result={},baseNames=Object.fromEntries((window.GALLERY_BOOTSTRAP?.workbench_contract?.classification_dimensions||[]).map(dimension=>[dimension.dimension_key,new Set((dimension.terms||[]).map(term=>classificationNameKey(term.term_name))) ])),add=(key,value)=>{const name=cleanClassificationName(value?.term_name||value);if(!baseNames[key]||!name||baseNames[key].has(classificationNameKey(name)))return;if(name.length>80)throw new Error('工作包中的分类标签超过80个字符。');const rows=result[key]||(result[key]=[]);if(rows.some(term=>classificationNameKey(term.term_name)===classificationNameKey(name)))return;rows.push({term_id:clientClassificationTermId(key,name),dimension_key:key,term_name:name,sort_order:10000+rows.length,active:1,customer_local:true})};for(const object of payload?.v4Objects||[]){if(object.object_type==='workspace_preferences')for(const[key,terms]of Object.entries(object.payload?.custom_classification_terms||{}))for(const term of Array.isArray(terms)?terms:[])add(key,term);if(object.object_type==='chart_organization')for(const[key,terms]of Object.entries(object.payload?.classifications||{}))for(const term of Array.isArray(terms)?terms:[])add(key,term)}return result}
const importedClassificationTermStoreBeforeMedia=importedClassificationTermStore;
importedClassificationTermStore=function(payload){const result=importedClassificationTermStoreBeforeMedia(payload);for(const object of payload?.v4Objects||[])if(object.object_type==='media_organization')for(const[key,terms]of Object.entries(object.payload?.classifications||{})){const rows=result[key]||(result[key]=[]);for(const value of Array.isArray(terms)?terms:[]){const name=cleanClassificationName(value?.term_name||value);if(!name||name.length>80||rows.some(term=>classificationNameKey(term.term_name)===classificationNameKey(name)))continue;rows.push({term_id:clientClassificationTermId(key,name),dimension_key:key,term_name:name,sort_order:10000+rows.length,active:1,customer_local:true})}}return result};
function mergeClassificationTermStores(current,incoming){const result=clone(current||{});for(const[key,terms]of Object.entries(incoming||{})){const rows=Array.isArray(result[key])?result[key]:[];for(const term of Array.isArray(terms)?terms:[])if(!rows.some(item=>classificationNameKey(item.term_name)===classificationNameKey(term.term_name)))rows.push(term);result[key]=rows}return result}
function localSettingEntryCount(value){if(Array.isArray(value))return value.length;if(value&&typeof value==='object')return Object.values(value).reduce((sum,item)=>sum+(Array.isArray(item)?item.length:item&&typeof item==='object'?Object.keys(item).length:1),0);return value==null||value===''?0:1}
function workspaceDraftImpact(){const settingsKeys=['yd-custom-domains','yd-showcase','yd-showcase-layout-draft-v2','yd-showcase-layout-order-v1','yd-chart-range-history','ghgy-display-name-overrides-v1','ghgy-chart-organization-overrides-v1',CLASSIFICATION_TERMS_STORAGE_KEY],settings=settingsKeys.reduce((sum,key)=>sum+localSettingEntryCount(safeJSON(workspaceGetItem(key)||'null',null)),0);return{charts:cachedCollection('yd-local-charts').length,media:cachedCollection('ghgy-local-media-drafts-v1').length,indicators:cachedCollection('ghgy-local-derived-drafts-v1').length,notes:Object.keys(parseObject('yd-chart-notes')).length,compositions:parseArray('yd-showcase-compositions-v1').length,folders:parseArray('yd-showcase-folders-v1').length,settings}}
function workspaceDraftImpactTotal(impact){return Object.values(impact||{}).reduce((sum,value)=>sum+Number(value||0),0)}
function workspaceDraftImpactSignature(impact){return stableJSON(impact)}
function renderWorkspaceClearImpact(impact){const fields={workspaceClearChartsCount:'charts',workspaceClearMediaCount:'media',workspaceClearIndicatorsCount:'indicators',workspaceClearNotesCount:'notes',workspaceClearCompositionsCount:'compositions',workspaceClearFoldersCount:'folders',workspaceClearSettingsCount:'settings'};for(const[id,key]of Object.entries(fields)){const target=byId(id);if(target)target.textContent=Number(impact?.[key]||0).toLocaleString('zh-CN')}}
function formalContentGuard(){const bootstrap=window.GALLERY_BOOTSTRAP;if(!bootstrap)throw new Error('专门清空功能只在客户浏览版中启用。');const indicators=bootstrap.indicators||[],charts=bootstrap.charts||[],derived=bootstrap.derived||[],media=bootstrap.media_assets||[],localLeak=[...indicators,...charts,...derived,...media].some(item=>String(item?.indicator_id||item?.chart_id||item?.derived_id||item?.media_id||'').startsWith('LOCAL-'));if(localLeak)throw new Error('正式内容边界校验失败：公开载荷中混入了本机身份，已停止清空。');return{bootstrap,indicators,charts,derived,media,counts:[indicators.length,charts.length,derived.length,media.length]}}
function assertFormalContentGuard(guard){const bootstrap=window.GALLERY_BOOTSTRAP,counts=[bootstrap?.indicators?.length||0,bootstrap?.charts?.length||0,bootstrap?.derived?.length||0,bootstrap?.media_assets?.length||0];if(!guard||bootstrap!==guard.bootstrap||bootstrap.indicators!==guard.indicators||bootstrap.charts!==guard.charts||bootstrap.derived!==guard.derived||bootstrap.media_assets!==guard.media||counts.some((value,index)=>value!==guard.counts[index]))throw new Error('正式内容保护校验未通过；页面已停止继续操作，请刷新后检查。')}
async function waitForWorkspaceWrites(){for(let round=0;round<20&&idbWriteQueues.size;round++)await Promise.allSettled([...idbWriteQueues.values()]);if(idbWriteQueues.size)throw new Error('仍有本机草稿正在保存，请稍后重新确认清空。')}
function clearLegacyDraftKeys(){for(const key of LEGACY_KEYS)localStorage.removeItem(key);const remained=LEGACY_KEYS.filter(key=>localStorage.getItem(key)!=null);if(remained.length)throw new Error(`浏览器旧草稿入口未能清除：${remained.join('、')}`)}
async function clearWorkspaceDraftStores(){const db=await openDB();await new Promise((resolve,reject)=>{const tx=db.transaction([CHART_STORE,DERIVED_STORE,MEDIA_STORE,RECORD_STORE,META_STORE],'readwrite');tx.objectStore(CHART_STORE).clear();tx.objectStore(DERIVED_STORE).clear();tx.objectStore(MEDIA_STORE).clear();for(const key of DURABLE_VALUE_KEYS)tx.objectStore(RECORD_STORE).delete(key);for(const key of WORKSPACE_DRAFT_META_KEYS)tx.objectStore(META_STORE).delete(key);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error||new Error('本机草稿事务清空失败。'));tx.onabort=()=>reject(tx.error||new Error('本机草稿事务已中止。'))})}
async function verifyWorkspaceDraftStoresEmpty(){const charts=await idbGetAll(CHART_STORE),derived=await idbGetAll(DERIVED_STORE),media=await idbGetAll(MEDIA_STORE),durable=[],meta=[];for(const key of DURABLE_VALUE_KEYS)if(await idbGetRecord(RECORD_STORE,key))durable.push(key);for(const key of WORKSPACE_DRAFT_META_KEYS)if(await idbGetRecord(META_STORE,key))meta.push(key);if(charts.length||derived.length||media.length||durable.length||meta.length)throw new Error('清空后的独立校验未通过；请不要继续使用当前页面，并联系维护人员。')}
function resetWorkspaceDraftCaches(){for(const url of chartObjectURLs.values())URL.revokeObjectURL(url);chartObjectURLs.clear();chartObjectURLSources.clear();storedRecordCache.clear();for(const key of Object.keys(HEAVY_COLLECTIONS))collectionCache.set(key,[]);for(const key of DURABLE_VALUE_KEYS)durableValueCache.set(key,null);pendingImport=null;lastSaveError=''}
function setWorkspaceActionDisabled(disabled){for(const id of['workspaceExport','workspaceExportFull','workspaceImportFile','workspaceClearDrafts','workspacePersistStorage']){const target=byId(id);if(target)target.disabled=Boolean(disabled)}for(const id of['workspaceMergeRestore','workspaceReplaceRestore']){const target=byId(id);if(target)target.disabled=Boolean(disabled)||!pendingImport}}
function syncWorkspaceClearConfirmation(){const confirmButton=byId('workspaceClearConfirm'),acknowledge=byId('workspaceClearAcknowledge'),phrase=byId('workspaceClearPhrase');if(confirmButton)confirmButton.disabled=workspaceClearInProgress||!acknowledge?.checked||String(phrase?.value||'').trim()!==WORKSPACE_CLEAR_CONFIRMATION}
async function openWorkspaceClearDialog(){await window.GHGY_WORKSPACE_READY;assertWorkspaceMutable();formalContentGuard();const impact=workspaceDraftImpact();if(!workspaceDraftImpactTotal(impact)){setWorkspaceStatus('当前浏览器没有可清空的本机草稿或本机设置。');return}pendingClearImpact=impact;renderWorkspaceClearImpact(impact);const dialog=byId('workspaceClearDialog'),acknowledge=byId('workspaceClearAcknowledge'),phrase=byId('workspaceClearPhrase'),status=byId('workspaceClearDialogStatus');if(acknowledge)acknowledge.checked=false;if(phrase)phrase.value='';if(status)status.textContent='第一步核对删除数量并勾选；第二步输入确认文字。';syncWorkspaceClearConfirmation();if(dialog&&!dialog.open)dialog.showModal()}
async function clearAllLocalDrafts(){const dialog=byId('workspaceClearDialog'),status=byId('workspaceClearDialogStatus'),confirmButton=byId('workspaceClearConfirm'),acknowledge=byId('workspaceClearAcknowledge'),phrase=byId('workspaceClearPhrase');if(!pendingClearImpact||!acknowledge?.checked||String(phrase?.value||'').trim()!==WORKSPACE_CLEAR_CONFIRMATION)return;workspaceMutationBlocked=true;workspaceClearInProgress=true;setWorkspaceActionDisabled(true);syncWorkspaceClearConfirmation();if(status)status.textContent='正在等待现有保存完成并再次核对数量…';try{await waitForWorkspaceWrites();const currentImpact=workspaceDraftImpact();if(workspaceDraftImpactSignature(currentImpact)!==workspaceDraftImpactSignature(pendingClearImpact))throw new Error('确认期间本机草稿数量发生变化，未执行删除。请关闭弹窗后重新核对。');const guard=formalContentGuard();if(status)status.textContent='数量复核通过，正在以单一事务清空本机草稿…';await clearWorkspaceDraftStores();clearLegacyDraftKeys();localStorage.setItem(WORKSPACE_CLEAR_SIGNAL_KEY,`${nowISO()}:${crypto.getRandomValues(new Uint32Array(1))[0]}`);await verifyWorkspaceDraftStoresEmpty();assertFormalContentGuard(guard);resetWorkspaceDraftCaches();renderWorkspaceClearImpact(workspaceDraftImpact());if(status)status.textContent='清空完成；正式内容保护校验通过。页面即将刷新。';setWorkspaceStatus(`已清空 ${currentImpact.charts} 张本机草稿图、${currentImpact.media} 张本机表格/图片、${currentImpact.indicators} 个本机草稿指标、${currentImpact.notes} 份文字、${currentImpact.compositions} 个组合和 ${currentImpact.folders} 个文件夹；内置正式内容未改变。`);setTimeout(()=>location.reload(),900)}catch(error){workspaceMutationBlocked=false;workspaceClearInProgress=false;setWorkspaceActionDisabled(false);if(confirmButton)confirmButton.disabled=true;if(status)status.textContent=`未执行或未完成清空：${error.message}`;setWorkspaceStatus(`本机草稿未清空：${error.message}`,true)}finally{if(!workspaceMutationBlocked){workspaceClearInProgress=false;syncWorkspaceClearConfirmation()}else if(dialog)dialog.dataset.completed='true'}}
async function previewFiles(files){pendingImport=await validatePackage(files);pendingImport.workspace_fingerprint=await workspaceFingerprint();const counts=pendingImport.manifest.counts||{},current=workspaceDraftImpact(),mode=pendingImport.manifest.export_mode||'full';setWorkspaceStatus(`预览通过（${mode==='incremental'?'增量包':'完整包'}）：${counts.indicators??counts.derived_drafts??0}个指标、${counts.charts??counts.chart_drafts??0}张图、${counts.media??0}张表格/图片、${counts.notes??counts.chart_notes??0}份说明、${counts.compositions??counts.showcase_compositions??0}个组合、${counts.folders??0}个文件夹；当前本机另有 ${current.indicators} 个指标、${current.charts} 张图、${current.media} 个 TB。尚未写入；合并会保留未冲突对象，替换会用所选包重建本机工作区。`);byId('workspaceMergeRestore').disabled=false;byId('workspaceReplaceRestore').disabled=false;byId('workspaceReplaceRestore').title='使用刚才预览通过的工作包替换当前本机草稿'}
function setWorkspaceStatus(message,error=false){const target=byId('customerWorkspaceStatus');if(target){target.textContent=message||'';target.dataset.kind=error?'error':'info'}}
function setWorkspaceReadiness(state,message){workspaceReadinessState=state;window.GHGY_WORKSPACE_READY_STATE=state;workspaceReadinessMessage=message;setWorkspaceActionDisabled(state!=='ready');setWorkspaceStatus(message,state==='error');if(state!=='pending')updateStorageUI()}
async function updateStorageUI(){const target=byId('customerStorageStatus');if(!target)return;try{const estimate=await navigator.storage?.estimate?.()||{},used=Number(estimate.usage||0),quota=Number(estimate.quota||0),reserve=quota?Math.max(50*1024*1024,quota*.1):0,remaining=Math.max(0,quota-used-reserve),backup=await getMeta('last_backup_at',null),saved=await getMeta('last_saved_at',null),charts=cachedCollection('yd-local-charts').length,media=cachedCollection('ghgy-local-media-drafts-v1').length,derived=cachedCollection('ghgy-local-derived-drafts-v1').length,notes=Object.keys(parseObject('yd-chart-notes')).length,compositions=parseArray('yd-showcase-compositions-v1').length;target.textContent=`本机草稿 ${charts} 张图、${media} 张表格/图片、${derived} 个指标、${notes} 份文字、${compositions} 个组合 · 已用 ${formatBytes(used)} · 安全可用约 ${formatBytes(remaining)} · 最近保存 ${saved?new Date(saved).toLocaleString('zh-CN'):'—'} · 最近备份 ${backup?new Date(backup).toLocaleString('zh-CN'):'从未导出'}${lastSaveError?` · 保存失败：${lastSaveError}`:''}`;target.dataset.kind=lastSaveError?'error':'info'}catch(error){target.textContent=`无法读取存储状态：${error.message}`;target.dataset.kind='error'}}
function formatBytes(value){if(!Number.isFinite(value)||value<=0)return'0 B';const units=['B','KB','MB','GB'],index=Math.min(units.length-1,Math.floor(Math.log(value)/Math.log(1024)));return`${(value/1024**index).toFixed(index?1:0)} ${units[index]}`}
function closeWorkspaceClearDialog(){if(workspaceClearInProgress)return;pendingClearImpact=null;const dialog=byId('workspaceClearDialog');if(dialog?.open)dialog.close()}
function bindUI(){
  const panel=byId('customerWorkspacePanel');if(panel)panel.hidden=!window.GALLERY_BOOTSTRAP;
  setWorkspaceActionDisabled(workspaceReadinessState!=='ready');setWorkspaceStatus(workspaceReadinessMessage,workspaceReadinessState==='error');
  byId('workspaceExport')?.addEventListener('click',()=>exportPackage('incremental').catch(error=>setWorkspaceStatus(error.message,true)));
  byId('workspaceExportFull')?.addEventListener('click',()=>exportPackage('full').catch(error=>setWorkspaceStatus(error.message,true)));
  byId('workspaceImportFile')?.addEventListener('change',event=>{pendingImport=null;byId('workspaceMergeRestore').disabled=true;byId('workspaceReplaceRestore').disabled=true;byId('workspaceReplaceRestore').title='请先选择工作包并完成预览';const files=[...(event.target.files||[])];if(files.length){setWorkspaceStatus('正在读取并校验所选工作包；预览通过前不会写入，也不能替换恢复。');previewFiles(files).catch(error=>setWorkspaceStatus(`工作包预览失败：${error.message}`,true))}else setWorkspaceStatus('尚未选择工作包；替换恢复保持不可用。')});
  byId('workspaceMergeRestore')?.addEventListener('click',()=>applyImport('merge').catch(error=>setWorkspaceStatus(error.message,true)));
  byId('workspaceReplaceRestore')?.addEventListener('click',()=>applyImport('replace').catch(error=>setWorkspaceStatus(error.message,true)));
  byId('workspaceClearDrafts')?.addEventListener('click',()=>openWorkspaceClearDialog().catch(error=>setWorkspaceStatus(`无法打开删除确认：${error.message}`,true)));
  byId('workspaceClearAcknowledge')?.addEventListener('change',syncWorkspaceClearConfirmation);
  byId('workspaceClearPhrase')?.addEventListener('input',syncWorkspaceClearConfirmation);
  byId('workspaceClearConfirm')?.addEventListener('click',()=>clearAllLocalDrafts());
  byId('workspaceClearCancel')?.addEventListener('click',closeWorkspaceClearDialog);
  byId('workspaceClearClose')?.addEventListener('click',closeWorkspaceClearDialog);
  byId('workspaceClearDialog')?.addEventListener('cancel',event=>{if(workspaceClearInProgress)event.preventDefault();else pendingClearImpact=null});
  byId('workspaceClearExportFirst')?.addEventListener('click',async()=>{const status=byId('workspaceClearDialogStatus');try{if(status)status.textContent='正在生成完整工作包，请在下载完成后再确认清空。';await exportPackage('full');if(status)status.textContent='完整工作包已生成；请确认文件已经保存到电脑，再继续双重确认。'}catch(error){if(status)status.textContent=`完整工作包未生成：${error.message}`}});
  byId('workspacePersistStorage')?.addEventListener('click',async()=>{try{const persisted=await navigator.storage?.persist?.();setWorkspaceStatus(persisted?'浏览器已允许持久存储。':'浏览器未授予持久存储；请定期导出工作包。');updateStorageUI()}catch(error){setWorkspaceStatus(error.message,true)}});
  updateStorageUI();
}
window.addEventListener('storage',event=>{if(![WORKSPACE_CLEAR_SIGNAL_KEY,WORKSPACE_MUTATION_SIGNAL_KEY].includes(event.key)||!event.newValue)return;workspaceMutationBlocked=true;setWorkspaceActionDisabled(true);const confirmButton=byId('workspaceClearConfirm');if(confirmButton)confirmButton.disabled=true;setWorkspaceStatus(event.key===WORKSPACE_CLEAR_SIGNAL_KEY?'另一个同网址页面已清空本机草稿。本页已停止继续保存，请刷新页面。':'另一个同网址页面已恢复工作包。本页已停止继续保存，请刷新页面。',true)});
window.GHGY_WORKSPACE={schemaVersion:SCHEMA_VERSION,setItem:workspaceSetItem,getCollection:cachedCollection,replaceCollection,saveChart:item=>saveCollectionItem('yd-local-charts',item),deleteChart:id=>deleteCollectionItem('yd-local-charts',id),saveDerived:item=>saveCollectionItem('ghgy-local-derived-drafts-v1',item),deleteDerived:id=>deleteCollectionItem('ghgy-local-derived-drafts-v1',id),saveMedia:item=>saveCollectionItem('ghgy-local-media-drafts-v1',item),mediaDeleteImpact:localMediaDeleteImpact,deleteMedia:deleteLocalMedia,exportCharts:()=>loadStoredCollection('yd-local-charts',true),exportMedia:()=>loadStoredCollection('ghgy-local-media-drafts-v1',true),snapshotImage,exportPackage,validatePackage,applyImport,updateStorageUI};const workspaceReady=(async()=>{try{const migrated=await migrateLegacy();setWorkspaceReadiness('ready','本机工作区检查完成；内置正式内容与“我的工作”均可使用。');window.dispatchEvent(new CustomEvent('ghgy-workspace-ready'));return migrated}catch(error){lastSaveError=error?.message||String(error);setWorkspaceReadiness('error',`内置正式内容仍可使用；本机工作区未能安全打开，已停止本机保存与恢复：${lastSaveError}`);window.dispatchEvent(new CustomEvent('ghgy-workspace-error',{detail:{error:lastSaveError}}));throw error}})();workspaceReady.catch(()=>{});window.GHGY_WORKSPACE_READY=workspaceReady;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bindUI,{once:true});else bindUI();
})();
